# 造价数据库 · 产品设计文档

> 版本：**v6.3**（FastAPI 适配版：技术路线从 Odoo 19 切换为 FastAPI + SQLAlchemy + Jinja2；目标用户从单人扩展为团队共享 + 公司网页嵌入；认证对接公司 OA 统一登录。业务契约、数据安全、界面设计 100% 继承 v6.2） · 日期：2026-09-05
> 技术细节见 `架构设计.md`，本文只讲**做什么、给谁用、先做哪些、看起来什么样**。
> **数据安全的技术实现见 `架构设计.md` 第四节**；本文第七节只讲**这些规矩对你意味着什么**。
> 原 Odoo 版 v6.2 已归档（git tag `legacy-odoo-final`），本文为 FastAPI 版适配。

## 一、一句话定位

**基于 FastAPI 的「历史造价数据查询库 + 物料标准化引擎」**——把散落各工程 Excel 清单里的综合单价沉淀成可查、可对比、可溯源的数据库；并解决老清单无国标编码时「靠名称+规格同类匹配」的难题。作为公司网页的一部分，支持团队共享查询。核心问题：

> 这个清单项，我们以前做过多少钱？它标准化后该归到哪一类？

## 二、解决什么问题

| 现状痛点 | 本产品的做法 |
|---|---|
| 报价靠翻旧 Excel，找一份要十几分钟 | 全部清单入一个库，按编码/名称/特征直接搜 |
| 同清单项在不同工程单价差多少，全凭印象 | 同编码/标准化项自动汇总，给均价/区间/样本数 |
| 审计对不上单价，说不清依据 | 每条记录可溯源到具体文件与工程，一键导出 |
| 综合单价脱离地区和时间无可比性 | 每条数据带省份 + 价格期，跨期对比自动分组 |
| **老清单大量无国标编码，靠名称对不上同类项** | **物料字典（三级分类）+ 相似度召回，做标准化归类** |
| **专业工具普遍"能用但难看难用"，不想打开** | **深空科技风格界面：干净、留白、字清楚，每天愿意用** |

## 三、目标用户

造价咨询从业人员（公司内部团队）。通过公司 OA 统一登录（易达 ECMS），多人共享同一数据库，按角色分权（admin 导入管理 / estimator 查询分析 / viewer 只读浏览）。可作为公司网页的一部分嵌入，团队成员通过公司门户直接访问。

## 四、三个核心场景

**场景 1 · 报价查历史价**：粘项目编码/名称搜 → 看历史单价区间 → 据此定价。

**场景 2 · 审计查异常单价**：导入待审清单（**自动标记为「待审」，绝不混入历史均价**，见 7.3）→ 与已完工程历史价横向对比 → 偏离区间标红 → 导出审核底稿。

**场景 3 · 数据沉淀与标准化**：做完工程把清单 Excel 导入 → 无编码项经字典匹配/相似度建议人工确认 → 沉淀为标准化历史单价。

## 五、功能清单

### P0（MVP，先跑通闭环）

| 功能 | 说明 |
|---|---|
| Excel 导入（稳健） | 向导支持国标表头；**合并单元格处理**、**汇总/合计行识别跳过**、异常行（空单价/零工程量/乱码）**预览标记**；**预览不写库，确认后才入库**；**【v6】重复导入同文件自动识别，只更新来自 Excel 的内容、保留人工标注**（不再"覆盖重来"） |
| **原始数据保护** | **【v6 P0】只读导入绝不改原文件；导入即归档（每条数据可点开当时那份 Excel）；删除进回收站可还原；改过什么留有删不掉的流水账**（详见第七节） |
| **数据性质标记** | **【v6 P0】导入时选「已完工程 / 控制价 / 投标报价 / 待审 / 信息价」；历史均价默认只用「已完工程」**（详见 7.3） |
| 物料字典维护 | **三级材料分类字典**（大类/系列/规格集合、同义词、规格白名单），UI 可编辑，不只在代码里 |
| 多条件组合查询 | 搜索视图：项目编码/名称/特征/标准化字段/单位/工程/省份/价格期组合筛选 |
| 列表与详情 | 列表视图（排序/分组）+ 表单视图看完整详情（含项目特征全文、标准化字段） |
| 结果导出 | 筛选结果集导出 Excel/CSV，**含原始字段 + 标准化字段 + 溯源字段**；**【v6】导出留记录，单次超 1000 行二次确认** |

### P1（紧接着做）

| 功能 | 说明 |
|---|---|
| 相似度候选匹配 | rapidfuzz 对未标准化项从字典召回 Top-N 候选，**仅建议、人工确认才回填**（绝不自动改数据） |
| 同类项历史单价统计 | **【v6】按统一"同类项聚合键"聚合**（字典 > 标准化 > 国标编码 > 原始名称，四级优先）；给样本数/最低/最高/平均/中位数；**必须显示口径构成**（本次均价里多少来自编码匹配、多少来自标准化），审计可解释 |
| 单价异常识别 | 与区间比较，偏离超阈值标红 |
| 工程维度视图 | 按工程看全部清单，支持层级折叠 |
| 导入批次管理 | 批次列表，查看/删除/重导；**【v6】回收站（还原误删批次）、批次校验和（一键核对"入库总额是否与 Excel 合计一致"）、归档完整性校验** |
| 审计日志查询 | **【v6】谁在什么时候把哪条数据从多少改成了多少，可查、可导出，删不掉** |
| 评估独立单价库 | M3 后评估是否新增 `cost.catalog` 归档标准化单价（MVP 用 boq_item 直接聚合） |

### P2（后续迭代）

| 功能 | 说明 |
|---|---|
| 向量语义检索 | LanceDB 外部独立库，语义召回候选（不侵入 PG） |
| 只读角色 | 多人时增加只能查询不能改的权限角色 |
| 其他 | 单价趋势、常用项收藏、标签分类、多库合并 |

## 六、明确不做（当前）

- 单项目 BOQ 编辑与编制
- CAD/BIM 算量、4D 排期、招投标管理
- 定额套价、人材机分析、造价指标库
- 云端 SaaS、对外公开访问

> **P3 后续扩展（前瞻，非当前范围）**：4D 排期与人材机分析。二者均可作为 Odoo 新模块扩展、复用同一 boq 数据。前提：4D 的 BIM 可视化需外部 BIM 软件配合（Odoo 承接其导出的进度/工程量做管理侧分析）；人材机分析**需引入可靠定额数据源**（与「数据禁编造」铁律一致，不能凭空生成）。

## 七、数据安全：原始文件才是命根子【v6 新增】

### 7.1 先纠正一个要命的误解

以前文档里写过一句「数据库是唯一数据源，原文件丢了就找不回来」。**这句话说反了，v6 予以推翻。**

真实关系是：

| 东西 | 到底是什么 | 丢了能重来吗 |
|---|---|---|
| **你那份原始 Excel** | **唯一的真相**。审计对账、扯皮举证，最终看的是它 | ❌ **不能** |
| **你手工填的标准化结果** | 你花时间判断出来的"这一项该归哪一类"，机器猜不出来 | ❌ **不能** |
| **数据库里的记录** | 从 Excel 解析出来的**索引**，方便查而已 | ✅ **能**——有原始文件，随时可以重新导一遍 |

所以本工具的安全原则只有一条：

> **数据库坏了不致命，原始文件和你手工填的东西丢了才致命。宁可数据库出问题，也不能动这两样。**

### 7.2 四条硬规矩

| 规矩 | 具体做法 | 防的是什么 |
|---|---|---|
| **① 只读，不改你的文件** | 导入时以"只读"方式打开；**永远不保存、不改名、不移动、不删除**你的原文件。含宏的文件（`.xlsm`）直接拒收 | 程序 bug 或误操作把原始 Excel 写坏、公式弄丢 |
| **② 导入即归档** | 点"确认入库"的那一刻，先把原文件**原封不动复制一份到受控目录**（只能加不能改），再写数据库。以后查任何一条单价，都能点开**当时那份文件、翻到对应的 Sheet 和行**——精确到"第几个单元格" | 源文件后来被别人改了、删了、挪走了，溯源断链 |
| **③ 重新导入，不带走你手工填的** | 同一个文件导第二次时，只更新"从 Excel 来的内容"；**你手工填的标准化结果，一个字都不动**。预览页会明确告诉你：新增几行、更新几行、**保住你的标注几行** | 辛苦标准化几百行，一重导全没了，界面还只说"已覆盖" |
| **④ 删掉的东西能找回来** | 删除只是"放进回收站"，不是真删；回收站里可一键还原。真要彻底删，得手动输入批次名确认，并且**系统先自动导出一份快照** | 手一抖点了删除，几千行数据 + 人工标注瞬间归零 |

### 7.3 一个必须分开的场景：待审清单不能混进历史价

场景 2 是"导入**待审**清单去对比查异常"。这里有个隐藏陷阱：

> 待审清单的报价是**别人的、没定稿的、可能虚高的**。如果它和"已完工程真实单价"混在一起算均价，**历史均价就被污染了**。下回你拿这个被污染的均价再去审新清单，误差会一轮轮放大。

**做法**：每条数据都带一个"**数据性质**"标记，导入时选：

| 性质 | 说明 | 算不算进历史均价 |
|---|---|---|
| **已完工程**（默认） | 结算/审计定案的真实单价 | ✅ 算 |
| 招标控制价 / 预算价 | | ❌ 不算（想算要手动勾） |
| 投标报价 | | ❌ 不算 |
| **待审清单** | **你的审计对象，不是历史依据** | ❌ **永远不算**，只当"被比对的那一方" |
| 信息价 / 市场价 | | ❌ 不算 |

**每次显示均价，界面上必须同时显示"样本数"和"这批均价是用什么性质的数据算出来的"**——不允许出现"不知道这个均价怎么来的"这种事。

> **导入时必须选，不能偷懒默认【v6.1】**：导入向导**强制你选一项"数据性质"，不选不让入库**，而且界面不替你预先勾选。否则一偷懒默认成"已完工程"，待审/控制价就悄悄混进了历史均价——前面这套防污染的设计在入口就白做了。

### 7.4 另外三件保障

| 项 | 做法 |
|---|---|
| **备份** | 每天自动备份数据库（保留 30 天）；原始文件归档目录另存一份到移动硬盘/异地。**异地那份要加密**——单价是商业敏感数据 |
| **归档文件也要锁好【v6.1】** | 你导入后自动归档的那份原始 Excel，本身就是商业敏感数据。**归档目录要放在加密盘（BitLocker/EFS）或至少限制只有本工具能读**——光设成"只读"不够，同电脑上别的账户照样能拷走 |
| **⚠️ 加密的钥匙要单独备份【v6.2】** | 加密盘/EFS 的**恢复密钥或证书，必须另外存一份到异地**（比如刻盘或存移动硬盘，和归档文件分开）。不然万一重装系统、证书丢了，**加密的那份原始 Excel 你自己都打不开**——这反而比"被别人拷走"更糟，等于把你的命根子自己锁死了 |
| **备份要演练** | **每季度真恢复一次**，抽 10 条和原始 Excel 逐字对一遍，结果记下来。**没演练过的备份，等于没有备份** |
| **谁改过，一查就知道** | 所有改动记流水账：谁、什么时候、把哪个数从多少改成了多少。**这本账谁都删不掉**（技术上禁止删除）。审计时可以出具"这条单价自导入以来从没被改过" |
| **不外泄** | 服务只监听本机（`127.0.0.1`），局域网访问不到；关闭数据库管理器对外入口；每次导出留记录（谁导的、导了多少行），单次导超过 1000 行要二次确认 |

> **一个提醒**：真实工程的 Excel **不要放进 Git 仓库**（万一仓库公开就泄露了）。测试用脱敏假数据，仓库里也要把 `*.xlsx`、`*.sql` 这类文件忽略掉。

## 八、数据来源与标准化

**只存自己导入的真实清单**，不引用、不生成任何未经核实的定额数据。

- 输入：各工程工程量清单 Excel（`.xlsx/.xls`），国标 12 位编码体系（老清单可无编码）
- 必备：项目编码（可空）、项目名称、项目特征、计量单位、工程量、综合单价、合价
- 可缺省：综合单价允许为空（约 12% 行无单价，统计自动排除）
- 地区与时间：province 手工/路径推断；`price_period` 用月份选择器录入（`YYYY-MM`）
- **标准化**：导入后对无编码/需归类的项，经物料字典匹配或相似度建议，**人工确认**后回填 `std_name/std_spec/material_dict_id`，作为可靠的同类项聚合键

## 九、界面设计语言（深空科技风格）【v5 新增】

### 9.1 为什么要有这一节

工具是每天要打开的东西。造价类软件常见的毛病是：信息堆满、字号小、颜色杂、分隔线到处都是，看十分钟就累。本产品明确采用 **深空科技设计语言**作为观感标准——不是要把 ERP 做成官网，而是取它"**干净、有呼吸感、重点突出**"的那套方法。

### 9.2 一句话概括观感

> **白底、大字、细分隔线、蓝色点缀、圆角卡片、轻阴影；没有斑马纹、没有粗边框、没有花哨渐变。**

### 9.3 五条视觉规矩

| 规矩 | 具体做法 |
|---|---|
| ① 颜色克制 | 整屏只有一种强调色（深空科技蓝 `#4D7CFE` + 青色 `#22D3EE`）；其余全是黑白灰。红只用于异常/错误，绿只用于正常 |
| ② 用留白代替框线 | 分组靠间距和淡灰底（`#F5F5F7`）区分，少画边框；分隔线用 1px 淡灰（`#D2D2D7`） |
| ③ 字要清楚 | 正文字号提到 14px，标题加粗到 600；长文本行高 1.5；数字用等宽对齐，方便上下比对单价 |
| ④ 圆角与轻阴影 | 卡片 12–18px 圆角 + 极轻阴影，营造"浮在纸上"的层次，而不是靠框线切格子 |
| ⑤ 动效轻快 | 过渡 160–280ms，缓动曲线统一；悬浮、展开、切换有反馈，但绝不拖沓 |

### 9.4 关键取舍：要风格，也要密度（重要）

深空科技方案是深色数据驾驶舱，适合长时间监控，我们的清单表是"一屏要看十几行"的工作台。**照搬官网的大留白会让一屏只剩 5 行，反而更难用。**

所以定死两条：

- **全量应用**：色彩、字体、字重、圆角、阴影、动效——这些与信息量无关，纯观感提升；
- **压缩留白**：官网的 80–120px 间距压到 **24–32px**；**列表行高不低于 40px，保证一屏能看到 ≥15 行清单**。

> 验收标准就一句话：**"深空科技风格观感 + 造价数据工作台实用性。"**

### 9.5 四个菜单长什么样

导航结构：**顶部导航**（全局搜索 + 用户）→ **四个菜单**：查询检索 / 单价分析 / 数据管理 / 物料字典。

| 菜单 | 观感 |
|---|---|
| **查询检索** | 顶部胶囊搜索框；已选条件显示为**可一键删掉的胶囊标签**（如 `北京 ×` `2026-03 ×`）；列表无斑马纹、悬浮轻高亮、鼠标移过就知道是哪行；点一行右侧展开详情卡片 |
| **单价分析** | 顶部四张**圆角指标卡**：样本数 / 均价 / 区间 / 异常数——大数字配小字说明；下面是透视表；超出区间的单价**红色底纹 + 图标**双标记（不只靠颜色，色弱也能分辨） |
| **数据管理** | 导入是一步步来的：顶部**步骤条**（上传 → 解析 → 预览 → 确认）；预览表格里异常行直接用黄/红底色标出来并写明原因（"单价为空""疑似合计行"）；**确认前一行数据都不入库** |
| **物料字典** | 左侧三级树（大类 → 系列 → 规格），右侧卡片式编辑同义词与规格白名单，不用改代码 |

**深色模式**同步支持：跟随系统设置，晚上看不刺眼。

### 9.6 一个要说清的现实：字体降级

深空科技观感很大一部分来自它的专用字体（英文 SF Pro、中文苹方）。这套字体**只在苹果设备上自带**。

- 在 **Mac / iPad** 上打开：观感 100% 到位；
- 在 **Windows（本项目主环境）** 上：自动降级为**微软雅黑 / Segoe UI**。

> 诚实预期：Windows 下**约 80% 接近**——字形骨架不同，但字号、字重、字距、留白节奏一致，整体依然协调清爽。我们**不内嵌字体文件**（体积大、许可复杂）；若日后确实想更接近，可再评估引入开源字体（如 Inter / 思源黑体），需单独确认许可与加载速度。

## 十、技术路线

**FastAPI + SQLAlchemy 2.0 + PostgreSQL 15 + Jinja2 模板**。从 Odoo 19 版迁移而来，业务契约与纯函数层（720 行）100% 继承。

金额/工程量文本保真 + `Decimal` 计算；rapidfuzz 做相似度候选（P1）；LanceDB 外部向量库（P2，不入库）；`data/aliases.py` 仅管 Excel 表头别名，物料同义词在 `material_dict` 模型。

**认证**：公司 OA 统一登录（易达 ECMS，待厂商确认 SSO 协议）；开发期独立账号占位，OA 对接到位后替换 `get_current_user`。三角色权限（admin/estimator/viewer）通过 FastAPI 依赖注入实现。

**呈现层**：深空科技设计语言全部实现在 `app/static/`（CSS 变量 + Jinja2 模板），前端 demo `app/static/demo/index.html` 从 Odoo 版原样复用（像素级视觉规格）。视觉验收用「8 张基线截图 + 核对清单」（见 `架构设计.md` 第九节）。

> **为什么从 Odoo 迁到 FastAPI**：Odoo 19 框架复杂度高、AI 写代码 bug 率高、前端定制困难、测试通道堵；FastAPI 业务代码 AI 写得好，且公司 OA 统一登录消除了"自建认证"短板。纯函数层零语言转换，资产复用率 ~80%。

> **合规说明**：借鉴的是 站酷参考案例公开的通用设计原则（排版、色彩层级、留白、圆角、动效），**不复制其任何产品代码、资源文件与商标资产**。

## 十一、里程碑

| 阶段 | 内容 | 完成标准 | 状态 |
|---|---|---|---|
| **M0 迁移准备** | FastAPI 骨架 + 纯函数迁移（720行）+ 90条测试全绿 + 前端 demo 复用 + Odoo 版归档（tag + pg_dump） | pure_tests 90 passed；FastAPI app 加载成功；demo 可访问 | ✅ 完成（2026-09-05） |
| M1 数据模型 | SQLAlchemy 四模型 + biz_id + 编码解析 + 分层 Upsert + 审计落库 + 三角色权限 | 模型可 create_all；安全验收 S1–S9 通过；权限用例通过 | ⏸ 待启动 |
| M1.5 设计系统 | 深空科技风格 token + 组件样式 + 深色模式（demo 已就绪，模板落地） | 核对清单通过（一屏 ≥15 行、正文 14px、主色 #4D7CFE） | ⏸ 依赖 M1 |
| M2 导入与查询 | 导入向导（只读打开 / 归档 / 分层更新 / 合并单元格 / 汇总行 / 异常标记 / 预览不写库）+ 搜索 + 导出 | 真实清单导入→查询→导出闭环；批次校验和与 Excel 合计一致；备份演练完成 | ⏸ 依赖 M1 |
| M3 单价分析 | KPI 卡片 + 仪表板（默认只用「已完工程」数据）+ 批次管理 + 回收站 + 匹配向导(P1) + 评估 cost.catalog | 自用一周无阻塞；均价可解释口径 | ⏸ 依赖 M2 |
| OA SSO 对接 | 易达 ECMS 统一登录对接，替换 `get_current_user` | OA 登录可直接访问门户；三角色映射正确 | ⏸ 待厂商文档（独立线） |
| P2 | LanceDB 向量检索 + 数据迁移（Odoo→新库 ETL） | 语义检索可用；迁移后数据零丢失 | 📋 规划中 |

> **为什么安全放在 M1 而不是以后补**：一旦库里存进真实工程数据，再回头补软删除和审计日志，中间发生的损失已经追不回来了。**安全是这个库的"地基"，不是装修。**

---

## 十二、协作约定（2026-09-04 用户指令，永久生效）

> 本节为跨里程碑的协作元规则，非产品功能设计；具体流程以 `AGENTS.md` 与 `项目总控.md` 为准。

1. **总控角色铁律**：总控（项目总控 Agent）只做记录与把控——维护《项目总控.md》、状态回写、阶段（泳道）划分、门禁定义、验收核对、调度派工；**不得亲自修改实现代码**。
2. **提交权收归总控**：具体改动（Python/SQLAlchemy/模板/配置）由执行 Agent 按泳道实施，每阶段独立交付；**git commit/push 一律由总控执行，执行 Agent 不 commit**。
3. **界面验收 = demo 完全一致**：真实后台观感以 `app/static/demo/index.html` 为**唯一像素级视觉规格**，逐场景复刻（侧栏/顶栏/卡片/表格/chip/步骤条/上传区），产品页完全一致。
4. **文件单一所有权**：每个文件只有一个负责 Agent；共享文件（main.py/config.py/requirements.txt）归 Agent-0 集成守门统一收口。
