# 造价数据库 · 架构设计

> 版本：**v7.0（FastAPI 适配版）**（从 v6.7 Odoo 版迁移：技术栈从 Odoo 19 切换为 FastAPI + SQLAlchemy 2.0 + Jinja2；认证从 Odoo 内置 ACL 切换为公司 OA 统一登录（易达 ECMS）+ 三角色依赖注入；部署从 Odoo 服务切换为 uvicorn + Nginx。**业务契约 100% 继承**：§四数据安全、§7.5 match_key、§7.6 审计日志、§十九 API 契约均原样保留，仅实现层适配）· 日期：2026-09-05
> 原 Odoo 版 v6.7 已归档：git tag `legacy-odoo-final`，数据库备份 `zaojia_db_20260905.dump`
> 产品视角见 `产品设计文档.md`
> 项目唯一事实源见 `项目总控.md`
>
> **⚠️ 动手写代码前必读第四节。** 本项目处理的是**不可重建的造价原始凭证**，第四节定义了「什么数据绝不能删、什么数据可被覆盖」的硬边界。不读完就写导入与删除逻辑，会造成不可逆的数据损失。
>
> **FastAPI 适配说明**：本文档中「Odoo ORM」「视图」「向导」「SCSS」「odoo-bin」等术语，在 FastAPI 版中分别对应「SQLAlchemy 模型」「Jinja2 模板页面」「多步表单页面」「CSS」「pytest」。字段设计、业务规则、安全机制、接口契约均不变。

## 一、定位（做什么、不做什么）

**做**：本地运行的**跨项目历史造价数据查询库 + 物料标准化引擎**。把散落各工程 Excel 清单里的综合单价沉淀下来，并解决一个现实痛点——**大量老清单没有国标 12 位编码，只能靠物料名称 + 规格做同类匹配**，因此必须有一套可维护的物料字典与相似度召回能力。

**不做**：单项目 BOQ 编辑与编制、CAD/BIM 算量、招投标、定额套价、人材机分析（后两项作为 P3 扩展方向，见 `产品设计文档.md`，前提是引入可靠定额数据源）。

## 二、技术栈

| 项 | 选择 | 理由 |
|---|---|---|
| Web 框架 | **FastAPI** | 异步高性能、自动 OpenAPI 文档、Pydantic 校验、AI 写代码质量高 |
| ORM | **SQLAlchemy 2.0** | 成熟稳定、显式事务、Alembic 迁移 |
| 数据库 | **PostgreSQL 15+** | numeric 精确，无 SQLite REAL 退化 |
| 模板引擎 | **Jinja2** | 服务端渲染，AI 最熟、维护成本低；可渐进升级为前后端分离 |
| 纯函数层 | `data/` 目录 720 行（从 Odoo 版原样复用） | 零框架依赖，90 条 pytest 全绿作为回归基线 |
| **呈现层** | **CSS 变量 + Jinja2 模板（`深空科技方案C`）** | 见第九节；前端 demo `app/static/demo/index.html` 从 Odoo 版原样复用 |
| 认证 | **公司 OA 统一登录（易达 ECMS）** + 三角色依赖注入 | 公司已有完整 OA，不建独立账号体系；开发期占位独立账号 |
| 相似度 | **rapidfuzz**（P1） | 老清单无编码时的字符串相似度候选召回 |
| 向量检索 | **LanceDB**（P2，外部独立文件库，**不入库 PG**） | 语义检索，避免加重数据库 |
| 许可 | **自有闭源** | FastAPI/SQLAlchemy/Jinja2 均 MIT/BSD，无传染义务 |
| 测试 | pytest（纯函数 + FastAPI 集成 TestClient） | 统一测试框架，比 Odoo `--test-enable` 简单可靠 |

**为什么从 Odoo 迁到 FastAPI【v7.0 迁移决策】**：Odoo 19 框架复杂度高、AI 写代码 bug 率高（ORM/views/OWL/SCSS 跨层）、前端定制困难、测试通道堵（全量测试被删除守卫杀死须分批）。FastAPI 业务代码 AI 写得好，且公司有 OA 统一登录消除了"自建认证"这一最大短板。纯函数层零语言转换，资产复用率 ~80%。

| 维度 | Odoo 19（原方案） | FastAPI（现方案） | 迁移收益 |
|---|---|---|---|
| 导入向导（预览不写库） | 原生 TransientModel | 自写多步表单 + Session | 代码更透明，AI 写得好 |
| 权限 / 多用户 | 原生 ACL + 多用户 | OA SSO + 三角色依赖注入 | 对接公司统一登录，无需自建账号 |
| 审计日志 / 软删除 | 原生 mail.thread + 自写 | 自写 append-only + SQLAlchemy 软删 | 逻辑更可控，无框架黑盒 |
| 可维护性 / 升级 | 每年一版、DOM 类名会变 | 依赖稳定，无框架升级压力 | 消除年度升级成本 |
| 前端定制 | OWL + SCSS，学习曲线陡 | Jinja2 + CSS，AI 最熟 | 前端 bug 率大幅降低 |
| 测试 | odoo-bin --test-enable，通道堵 | pytest，简单可靠 | 测试可全量跑，无守卫杀死问题 |

> 结论（v7.0 锁定）：FastAPI 选型的真实依据是「团队共享 + 公司网页嵌入 + AI 写代码质量」——OA 统一登录消除了认证短板，SQLAlchemy/Jinja2 是 AI 最熟的技术栈，纯函数层可零成本复用。付出的代价是「导入向导/权限/审计需自写」，但这些业务逻辑在 Odoo 版已经设计清楚，迁移只是实现层替换。

## 三、许可边界（FastAPI 版）

FastAPI（MIT）、SQLAlchemy（MIT）、Jinja2（BSD）、PostgreSQL（PostgreSQL License）均为宽松许可，**无传染义务**。本项目业务代码完全自写，可闭源。

> **呈现层许可边界**：深空科技方案C指**公开的通用视觉原则**（排版、色彩层级、圆角、留白节奏、动效曲线）。仅借鉴设计语言，**不复制 参考案例的任何产品代码、资源文件与商标资产**；字体不内嵌，走系统字体栈（见 9.4）。
> 前端 demo `app/static/demo/index.html` 从 Odoo 版原样复用，为自有代码，无第三方许可问题。

## 四、数据安全 · 原始数据保护（第一性原理）【v6 新增 · P0】

### 4.1 先纠正一个根本认知：谁才是「源」

v5 及更早版本写过一句话：「DB 是唯一数据源，原文件丢失不可恢复」。**这句话因果倒置了，v6 予以推翻。** 回到第一性原理重新分类：

| 对象 | 本质 | 能否重建 | 保护优先级 |
|---|---|---|---|
| **原始 Excel** | **真相源（Source of Truth）**：审计凭证、法律依据、可逐字对账 | ❌ **不能** | **最高** |
| **人工标注** | **人工智慧资产**：花时间判断出来的标准化/归类/异常判定 | ❌ **不能** | **最高** |
| **数据库记录** | **派生物（Derived Data）**：从 Excel 解析出的索引与视图，**天然有损** | ✅ 能（有原始文件 + 代码即可） | 次高 |

**数据库为什么是"有损派生物"**：它丢掉了单元格公式、合并单元格的视觉结构、批注与修订痕迹；未识别列虽进 `extra` 但不保证完整；解析规则一旦变更，同一份 Excel 会解析出不同结果。

因此本项目安全策略的**第一原理**是：

> **保护原始文件 + 保护人工标注，优先级高于保护数据库。数据库可以从前者重建，前者永远无法从数据库重建。**

### 4.2 四层安全目标（逐条落到设计）

| 层 | 目标 | 现实威胁 | 措施 |
|---|---|---|---|
| **完整性** | 原始数据不被改、人工资产不被静默摧毁 | 导入代码误写源文件；重导覆盖人工标注 | 4.3 只读导入；4.5 分层保护 |
| **可用性** | 数据不丢、丢了能找回 | 误删批次、硬盘故障、数据库损坏 | 4.7 软删除；4.9 备份与演练 |
| **可追溯** | 任何数字能回原始文件；任何修改能查到人 | 无法审计、无法回滚、无法自证清白 | 4.4 归档；4.10 审计日志；4.11 校验和 |
| **机密性** | 商业敏感单价不外泄 | Odoo 默认监听 `0.0.0.0`；导出失控；测试数据入 Git | 4.12 网络与导出管控 |

### 4.3 只读导入（最容易忽略、后果最严重的一条）

openpyxl 的 `load_workbook()` 默认 `read_only=False, data_only=False`，且**工作簿对象可 `save()`**。任何一处误写、或任何拿到 wb 对象的代码，都可能**直接覆写用户原始 Excel**——而原始文件不可重建。

**硬规则（写进代码注释与 Code Review 清单）**：

| 规则 | 要求 |
|---|---|
| 打开方式 | 必须 `load_workbook(path, read_only=True, data_only=True)` |
| 禁止保存 | `import_service` **不得持有可写工作簿**，代码中**永不对源文件句柄调用 `save()`** |
| 禁止原地操作 | 不修改、不重命名、不移动、不删除源文件 |
| 格式白名单 | **仅 `.xlsx`**；**拒收 `.xlsm` / `.xlsb`（含宏）**；`.xls` 提示先另存为 `.xlsx` |
| 规模上限 | 单文件 ≤ 50MB、单 sheet ≤ 20 万行（防 zip bomb / 内存耗尽），超限拒绝并说明 |
| 外部链接 | 检测到外部工作簿链接时**警告**（数值可能是缓存值而非实时值） |

> **为什么 `read_only=True` 能真正防 `save`**：`read_only=True` 时 openpyxl 以只读方式打开底层文件且**根本不支持 `save()`**——一旦调用会抛 `InvalidFileException`。这才是真正的防线（而非仅靠代码审查红线禁 `wb.save(`）。因此「只读打开」是**机制保障**，代码审查只是双保险。

> **`data_only=True` 的副作用——不能放松警惕**：读公式单元格时取的是 Excel 上次保存的**缓存值**；若文件从未被 Excel 打开并保存过，缓存为空，`None`。此处文档 v6 曾写"对读别人导出的清单基本无影响"——**这是一个未验证的假设**。造价软件（广联达/智多星等）导出的 xlsx 常在公式缓存为空时只留公式本身，此时单价/合价等**关键数值列可能整列为 `None`**。
> **【v6.2 强化】**：解析完成后，若 `unit_rate`/`total` 等关键数值列**整列为空且文件含公式**，必须**明确报错或强告警并阻断入库**，不得仅"预览提示"。否则会静默导入一批全部无单价的垃圾数据。

### 4.4 原始文件归档：让「溯源」名副其实

`source_path` 只存路径是不够的——文件会被移动、重命名、覆盖、删除。等到要审计时打开路径，文件早已不在。

**方案：导入即归档（Archive-on-Import）**

| 环节 | 做法 |
|---|---|
| 归档位置 | 受管目录 `<archive_root>/YYYY/MM/<file_hash[:2]>/<原文件名>`，**放在 Odoo 之外** |
| 归档时机 | **先可重入地成功复制归档（幂等，不覆盖已有同名归档），再写库**；任一步失败 → 整批中止，**已复制的归档文件标记待清理或留待同 `file_hash` 重导幂等复用，不留孤儿**（见下方原子性说明） |
| 归档内容 | 原始文件字节流（不可变）+ 同名 `.meta.json`（原路径、大小、SHA256、导入时间、批次 ID、操作人） |
| 写保护 | 归档目录对日常账号**只读**（去掉写/删权限），只能追加 |
| **静态机密性【v6.1】** | 原始文件属商业敏感数据，归档目录应置于 **BitLocker / EFS 加密卷**，或至少以 **NTFS ACL 限制为仅 zaojia 服务账号可读**——防同机其他账户/管理员窥探（与 4.12「造价数据商业敏感」立场一致）。**v6 仅要求"只读"，未要求加密，属遗漏**：只读只防误改，不防有读权限的人拷走 |
| **⚠️ 加密密钥必须异地备份【v6.2 → v6.5 补演练】** | **EFS 证书 / 加密盘恢复密钥必须与归档文件本身分开异地备份**。否则密钥一旦丢失（重装系统、证书损坏），加密后的真相源将**永久无法打开**——这等于制造了新的"不可重建"风险，与 4.1 第一原则直接相悖。加密提升了机密性，却把"可用性"押在了密钥管理上，必须在 M0 规划时一并交付密钥备份方案。**【v6.5 补】密钥恢复须与归档恢复同等演练**：每季度恢复演练（4.9）须同时验证「持备份密钥能成功解密一份归档样本」，**未实际演练过的密钥恢复等同无效**（与"未演练过的备份视为无效"同一第一性原理）。密钥"异地"指**不同物理位置**（非同机不同文件夹），机器报废时密钥仍须可取。 |

> **分层加密总览（定位用）**：① 原始 Excel 归档 → BitLocker/EFS 加密卷（**密钥异地备份**，防反噬，见上）；② 数据库 `pg_dump` → 异地份 7z AES-256 加密（4.9）；③ 备份文件 → 同上加密。三层均遵循"密钥与密文分开异地"原则。EFS 绑定 Windows 用户证书，服务端/PG 场景须改用 BitLocker 或独立密钥管理，避免证书丢失即锁死真相源。

| 完整性校验 | 提供「校验归档」功能：重算每个归档文件 SHA256，与 DB 中 `file_hash` 逐项比对，列出不一致 |
| 数据库 | `import_batch.archive_path` 存归档相对路径；`source_path` 保留原路径**仅供人读参考** |

> **跨系统原子性（v6.2 纠正措辞）**：v6 曾写"归档与写库同一事务"。严格说**文件系统复制与 PostgreSQL 事务不是一回事**——文件复制不受 DB 事务回滚控制。正确实现是：**归档复制先行且幂等（同 `file_hash` 不覆盖、可重入）**；归档成功后写库；若写库失败回滚，已复制的归档文件**无害残留**（下次同文件重导会复用，不重复复制）。因此"整批中止"指**结果一致（要么都成、要么都不生效于用户可见数据）**，而非靠单一事务保证。这避免了"DB 回滚了、归档却留了孤儿"的假原子性幻觉。

> **为什么不用 Odoo 的 `ir.attachment` 存原始文件**：附件进 filestore 后路径由 Odoo 托管，会受 Odoo 附件真空清理、模块卸载、数据库重建影响。造价原始凭证需要的是"我自己能牢牢看住的目录"——**Odoo 崩了、重装了、换版本了，文件还得在。**

### 4.5 可重建数据 vs 不可重建数据（本设计最关键的一条）

**v5 的幂等策略「同 `file_hash` 先删旧批次全部 boq_item 再写」是危险设计，v6 予以推翻。**

一个真实会发生的场景：

1. 导入批次 A（1000 行）；
2. 花两小时人工标准化了其中 300 行（填 `std_name` / `std_spec` / `material_dict_id`）——**这 300 行的价值远高于其余 700 行**；
3. 因故重导同一文件（补几行、或单纯忘了导过）；
4. → **300 行人工标注被级联删除，永久丢失**，而界面只提示"已覆盖"，不提示"你失去了什么"。

**第一性原理修正：删除与覆盖策略必须按「能否重建」分层。**

| 类别 | 字段 | 能否从 Excel 重建 | 重导时处理 |
|---|---|---|---|
| **A 类 · 可重建** | `item_code` / `item_name` / `item_feature` / `unit` / `ordinal` / `sub_division` / `sequence` / `quantity*` / `unit_rate*` / `total*` / `provisional_sum*` / `classification` / `extra` | ✅ 能 | **允许覆盖更新** |
| **B 类 · 不可重建** | `std_name` / `std_spec` / `material_dict_id`、人工改写的 `anomaly_flag` / `anomaly_reason`、`data_source_type`、人工备注 | ❌ 不能 | **禁止删除、禁止静默覆盖** |
| **C 类 · 系统元数据** | `import_batch_id` / `file_hash` / `create_date` / `write_date` / `active` | — | 随批次管理 |

> 一句话落地规则：**重导永远只能"增加或修正从 Excel 来的东西"，绝不能带走人脑产出的东西。**

### 4.6 幂等策略改为分层 Upsert

| 步骤 | 规则 |
|---|---|
| 匹配键 | 优先 `(import_batch_id, sequence)`；未命中再按 `(item_code_norm, item_name_norm, unit)` 匹配（应对原文件行序调整） |
| A 类字段 | 命中则**更新**；未命中则**新建** |
| B 类字段 | **永不覆盖**：已有值原样保留；仅在该行为新建、或该字段为空时才写入 |
| 行数减少 | 原 1000 行、新导入 980 行 → **不自动删那 20 行**，进 `orphaned` 待确认列表，由用户在界面决定「保留 / 软删除 / 转存为独立批次」 |
| 预览页必显 | 新增 N 行 / 更新 M 行 / 孤儿待确认 K 行 / **保留人工标注 X 行** —— 四项计数缺一不可 |

### 4.7 软删除与回收站（防误删）

v5 的「删除批次 → 级联删除其 boq_item」同样危险：一次误点 = 几千行清单 + 人工标注全没。**v6 改为软删除。**

| 规则 | 做法 |
|---|---|
| 默认软删除 | `import_batch.active` / `boq_item.active`（默认 True）；删除 → 置 False，列表默认过滤，**不物理删** |
| 回收站视图 | 「数据管理 → 回收站」可见已删除批次，支持**一键还原** |
| 硬删除闸门 | 二次确认 + **必须手动输入批次名**；执行前**自动导出全量 CSV 快照**到归档目录 |
| 人工资产闸门 | 若待删批次含 B 类数据，**强制**先展示「将丢失 X 条人工标注」并要求导出快照，否则不允许继续 |
| 级联范围 | 软删除批次时其 boq_item 一并软删除（可整体还原） |

**状态字段语义（防实现歧义）【v6.2 厘清】**：本设计有三类布尔状态，必须分清：
- `active`：**软删除的唯一判据**。`False` 表示该行"逻辑删除"，列表默认过滤。
- `orphaned`：**仅由重导产生**（原批次有、新导入无的行）。它**不是独立状态**，而是 `active=False` + `orphaned=True` 的待确认行，统一进回收站/待确认列表，由用户在界面决定保留/软删/转存。
- `anomaly_flag`：表达**数据质量**（warning/error），与删除状态**正交**——异常行仍 `active=True`、正常参与查询与统计（除非被排除的空单价等）。
> 三者关系不清会在 M1 实现时导致"孤儿行到底 active 不设、校验和算不算它"的混乱。校验和（4.11）明确只数 `active=True` 的行。

### 4.8 数据性质隔离：待审清单不得污染历史单价库

`产品设计文档.md` 场景 2 写的是「**导入待审清单** → 同编码横向对比 → 偏离区间标红」。这里隐藏一个统计污染问题：

> 待审清单的报价（别人的、未定稿的、可能虚高的）一旦入同一张表，**会被算进"历史均价"**。下一轮拿这个被污染的均价去审新的清单，误差会自我放大。

**修正：新增 `data_source_type` 字段，区分数据性质。**

| 取值 | 含义 | 是否计入历史单价统计 |
|---|---|---|
| `completed` | **已完工程实际单价**（结算/审计定案）—— 真正的"历史价" | ✅ **默认计入** |
| `control_price` | 招标控制价 / 预算价 | ❌ 默认排除，需显式勾选 |
| `bid_price` | 投标报价 | ❌ 默认排除 |
| `pending_review` | **待审清单**（审计对象，不是历史依据） | ❌ **永远排除**，仅作为被比对方 |
| `info_price` | 信息价 / 市场价 | ❌ 默认排除 |

**硬规则**：所有聚合（均价/区间/中位数）的**默认域**为 `data_source_type = 'completed' AND unit_rate_num IS NOT NULL`。UI 上每次展示统计结果时**必须显示样本数与数据性质构成**，不允许出现"不知道这个均价是拿什么数据算出来的"这种情况。

**【v6.1 新增】导入强制选择，禁止静默默认**：模型层 `completed` 默认值**仅作兜底**；**导入向导必须显式要求用户选择 `data_source_type`，且 UI 不得预填**——未选则阻断入库。理由：若向导默认 `completed`（最宽松、会进历史均价的值），用户"忘了选"就会把待审/控制价默默混进历史均价，§4.8 防污染的努力在导入入口就失效了。

### 4.9 备份与恢复演练（3-2-1 + 演练）

v5 只写了"定期 pg_dump + 提供说明文档"。**没有验证过恢复的备份，等于没有备份。**

| 项 | 要求 |
|---|---|
| **3-2-1** | **3 份副本**（生产库 + 本地备份 + 异地/移动硬盘）／**2 种介质**／**1 份离线或异地** |
| **频率** | 数据库：每日自动 `pg_dump`，保留 30 天滚动；归档原始文件：与导入同步（本就不可变，只需整体同步） |
| **自动化** | Windows 计划任务跑备份脚本，**失败必须出声**（写日志 + 桌面通知），不允许静默失败 |
| **加密** | 备份含商业敏感单价，**异地份必须加密**（如 7z AES-256），密码不进仓库 |
| **恢复演练（关键）** | **每季度一次**：在临时库完整恢复，抽样比对 **10 条**记录的金额与原始 Excel **逐字一致**，结果记入 `docs/backup_drill.md`。**未演练过的备份视为无效** |
| **RPO / RTO** | RPO ≤ 1 天（原始文件不可变、可重导，丢一天工作量可接受）；RTO ≤ 4 小时 |
| **禁止入仓** | `*.sql` / `*.dump` / `filestore/` / 真实工程 Excel 全部加进 `.gitignore`，**严禁 commit**（见 4.12） |

### 4.10 不可变审计日志（可追溯的最后一块）

Odoo 原生 `mail.thread` 能记消息，但字段级追踪需逐字段 `tracking=True`，且 `mail.message` 可被管理员删除，不满足**可追溯**要求。**新增模型 `zaojia.audit.log`（append-only；核心价值是「防手误 + 可追溯」，而非对抗恶意篡改）**：

| 字段 | 说明 |
|---|---|
| `model` / `res_id` | 被操作对象 |
| `action` | `create` / `write` / `soft_delete` / `restore` / `import` / `export` |
| `field_name` / `old_value` / `new_value` | **字段级**变更（write 时逐字段记录） |
| `user_id` / `timestamp` / `batch_id` | 谁、何时、属于哪一批 |
| `reason` | 人工操作可填原因（如"依据 2026-08 结算书调整"） |

| 规则 | 做法 |
|---|---|
| 只追加 | 不提供 update / unlink 的 UI 与权限；模型层 `write()` / `unlink()` **直接抛异常** |
| 覆盖范围 | `boq_item` 全部 write（**尤其 B 类字段**）、`import_batch` 导入/软删除/还原、**全部导出动作** |
| 清理策略 | 默认永久保留；确需归档时按年加密导出并在日志中记录归档位置，**不静默删** |
| 核心价值 | ① **防手误**：误改可回滚到具体旧值；② **可追溯**：审计时可出具"这条单价自导入以来从未被修改"的证明；③ 事后追责定位。对**单人本机工具**，真实价值是「防自己手误 + 可解释」，而非对抗恶意篡改（见下方防篡改边界） |

**防篡改边界（诚实声明）【v6.2】**：上述三重保障防的是**应用层**（Odoo 业务逻辑与 UI）篡改；**不防直接连数据库改表**（如 DBA / 管理员直连 PG）。对单人本地工具，辅以「数据库角色仅服务账号有写权限、日常操作账号无 `audit_log` 写权限」即可将风险降到可接受。若需更强保证，可后续引入**哈希链**（每条日志记录含前一条哈希），使事后能检测任意篡改——MVP 不实现，列入 P2 评估。

**B 类字段改 `reason` 的实现机制【v6.2】**：原生 form 保存走 `write()` 不会自动弹 `reason`，所以"要求填原因"不能只是一句空话。实现上：
1. B 类字段（`std_name`/`std_spec`/`material_dict_id`/`anomaly_*`/`data_source_type`/人工备注）在表单设为 `readonly`，**仅通过专用向导**（如"修改标注"向导）修改，向导强制填 `reason` 才调 `write`；
2. `boq_item.write()` 覆写中，若检测到 B 类字段变化而无关联 `audit_log` 记录，则拒绝并提示"请走专用标注向导"——把"要求"变成**机制强制**。

### 4.11 批次校验和（检测数据漂移与篡改）

导入完成时固化三项校验值，存于 `import_batch`，可随时复算比对：

| 校验项 | 定义 | 用途 |
|---|---|---|
| `checksum_count` | 入库行数 | 检测意外增删 |
| `checksum_total` | `sum(total_num)`（`Decimal` 计算后取 2 位） | **与 Excel 合计行比对**（容差 0.01 元）—— 审计基本动作 |
| `checksum_hash` | 按 `sequence` 升序拼接 `item_code|item_name|quantity|unit_rate|total` 后取 SHA256 | 检测任何静默改动 |

> 提供「重新校验本批次」按钮：重算三项与存值比对，不一致即在界面高亮并列出差异行。这是**发现数据被误改或程序 bug 的最快手段**。

### 4.12 机密性（造价数据是商业敏感数据）

| 风险 | 措施 |
|---|---|
| **Odoo 默认监听 `0.0.0.0:8069`**，局域网可达 | 配置绑定 **`127.0.0.1`**；确需局域网访问时配防火墙白名单 |
| **Database Manager 对外暴露**（可列库、删库、下载备份） | 生产库 **`list_db=False`**；`admin_passwd` 用强随机值，**不进 Git** |
| 弱口令 / 共用 admin | 数据库角色强密码；日常操作**另建个人账号**，不用 admin |
| 导出失控 | 导出记入审计日志（谁、何时、筛选条件、行数）；**单次 >1000 行需二次确认** |
| 测试数据泄露 | 真实工程 Excel **禁止入库 Git**：`.gitignore` 增加 `test_data/`、`*.xlsx`、`*.sql`、`*.dump`、`filestore/`；集成测试用**脱敏夹具**（工程名替换、单价扰动） |
| 依赖供应链 | `openpyxl` / `rapidfuzz` / `LanceDB` **锁定版本**（requirements 写 `==`），升级需留变更记录 |

### 4.13 安全验收清单（M1 必测，缺一不可）

| # | 验收项 | 判据 |
|---|---|---|
| **S1** | 导入不改源文件 | 导入前后源文件 **SHA256 一致** |
| **S2** | 源文件已归档 | 归档目录存在该文件，且 SHA256 与 `file_hash` 一致 |
| **S3** | 重导不丢人工标注 | 先标准化 10 行 → 重导同一文件 → 10 行标注**仍在** |
| **S4** | 删除可回收 | 软删除批次 → 回收站可见 → 还原后数据完整 |
| **S5** | 修改可追溯 | 改一条单价 → 审计日志出现 `old → new` 记录 |
| **S6** | 批次校验和 | `sum(total_num)` 与 Excel 合计行一致（容差 0.01 元） |
| **S7** | 待审数据不污染 | 导入 `pending_review` 批次后，历史均价**不变** |
| **S8** | 备份可恢复 | 演练：临时库恢复成功 + 抽样 10 条与原始 Excel 逐字一致 |
| **S9** | 不外泄 | Odoo 绑定 127.0.0.1、`list_db=False`、仓库内无真实数据文件 |

### 4.14 预期负载与规模假设【v6.5 新增 · P1-2 修正】

> 审查指出：原设计所有「性能差 / 数据库负担重」的论断（GROUP BY 慢、PG 不应被向量加重、导入上限 50MB/20万行）**都缺规模假设**，使性能决策成为空中楼阁。本节补上明确数字，所有性能相关决策挂在这组假设上；**数字变了，决策要能重算**。

**规模假设（明确标注为假设，非实测）**：

| 维度 | 典型小组/个人库 | 中型造价咨询公司（上线开放后） | 突破阈值（触发重估） |
|---|---|---|---|
| 工程数 | 50–300 | 300–2000 | > 5000 |
| 每工程清单行 | 5千–3万 | 同左 | — |
| `boq_item` 总行数 | **10万–50万** | **300万–3000万** | **> 5000万** |
| 年增长 | 数万行 | 数十万–数百万行 | — |
| 并发用户 | 1 | 10–50（同公司内网） | > 200 |

**挂在规模上的决策（可重算）**：

- **历史价聚合**：在「典型 / 中型」量级下，PG 对 `GROUP BY aggregate_id` 是**毫秒级**（`aggregate_id` 有索引）；**默认不建 `cost.catalog` 预聚合表**（见 §7.4 / M4 §3），用按需计算即可。仅当突破阈值（> 5000万行且实时聚合不可接受）才启用。
- **向量检索**：在「中型」量级下，`material.dict` 同义词 + rapidfuzz 足以覆盖绝大多数匹配；**LanceDB 默认不启用**（见 §十二），除非召回率实测不达标。
- **导入上限 50MB / 20万行**：保留为**防御性**上限（防 zip bomb / 内存耗尽），不与规模假设挂钩，仍有效。
- **`_parent_store`**：M1 §4.6 的取舍——`material.dict`（小表、读多写少）**已启用** `_parent_store`（Odoo 19 须显式声明 `parent_path`）；`boq.item`（大表、批量写多）**暂不启用**，靠 `import_batch_id + sequence`，数据量超阈值再评估。

> 这组数字是**估算假设**，不是承诺。M0 装环境后应以真实样例回填「实际行数 / 单工程规模」，若与假设偏差一个数量级，须重算上述决策并在本表标注。

### 4.15 产品级成功指标【v6.5 新增 · P2-5 修正】

> 审查指出：里程碑有「完成标准」但无「用户问题是否被解决」的指标。以下为产品价值验证指标，应在 M3 上线后 4 周内采集基线并持续跟踪（§4.1 自身也强调「未演练=无效」，此处把同一标准要求扩展到产品价值）：

| 指标 | 目标（建议，待 M3 实测校准） | 说明 |
|---|---|---|
| 查一个历史价耗时 | < 3 秒（含筛选 + 结果渲染） | 核心动作，慢则工具失效 |
| 匹配召回率 | ≥ 90%（高价值项） | 已标准化/可匹配项中被正确归并的比例 |
| 误合并率 | < 2% | 不同物料被并入同一 `aggregate_id` 的比例（审计红线） |
| 人工标注工时 | 较「全人工」下降 ≥ 60% | 稀缺资源是标注时间，须用高置信自动链接减负（见 M3 §5.6） |
| 重导零损失 | 100% | 人工标注在重导后仍在（S3）且不孤儿失联（见 M2 §3.1） |

## 五、借鉴 OCE 的六项决策（沿用，适配 Odoo）

| # | 决策 | Odoo 映射 |
|---|---|---|
| 1 | 金额/工程量以文本存、业务层转 Decimal | `Char` 存原始文本（quantity/unit_rate/total/provisional_sum）+ `Float(16,6)` 计算字段排序/聚合；Python `Decimal` 算术 |
| 2 | 行号与项目编码分离 | `ordinal`(Char) + `item_code`(12 位国标)；统计以 `item_code+unit` 聚合 |
| 3 | 层级 + 复合索引 | `parent_id` 自引用 + `sequence`；`material.dict` 启用 `_parent_store=True`（boq.item 暂不启用，见 4.14） |
| 4 | 乐观锁 version | **复用 Odoo 原生并发（write_date + ORM 写锁）**；`version` 仅作业务层版本追踪，不重复造乐观锁 |
| 5 | JSON 扩展列 | Odoo 原生 `fields.Json`：`classification`、`extra` |
| 6 | 列别名导入 | `aliases.py` 维护「规范列名 → 中文/英文表头别名」，专注 **Excel 表头**映射（物料业务同义词另放数据库，见 7.3） |

## 六、OCE 空白 = 我们的增量

1. **中文清单表头识别为零**：OCE 别名表无 `项目编码/名称/特征/单位/工程量/综合单价/合价`，本模块以中文为主建立映射。
2. **不持有国标规范正文**：同 OCE，不做定额套价，只沉淀已完工程实际单价。
3. **省份 + 价格期（OCE 无）**：单价离开地区与时间无可比性，`province` + `price_period`（见 7.1）。
4. **面向国内造价场景的界面观感（v5 新增）**：OCE 沿用 Odoo 默认企业后台观感；本模块建立自有设计系统，见第九节。

## 七、数据模型（SQLAlchemy 2.0，从 Odoo ORM 迁移）

> **FastAPI 适配说明**：以下模型字段设计 100% 继承 Odoo 版，仅 ORM 实现从 Odoo `models.Model` 切换为 SQLAlchemy `DeclarativeBase`。表名去掉 `zaojia_boq_` 前缀（如 `zaojia_boq_boq_item` → `boq_item`），字段名保持一致。`biz_id`（UUID）作为稳定业务 ID，替代 Odoo 自增 `id` 作 API 标识。

### 7.1 `zaojia.boq.item`（清单项，核心）

> **Odoo 19 实测（M1.1）**：`fields.Char` 已移除 `size` 参数（列定义不体现长度，PG 为无上限 varchar）。下表 `Char(n)` 的 `n` 仅为**业务长度约定**；需强约束处（如 `biz_id`=36）由业务校验/unique index 保证。复合索引用声明式 `models.Index`/`UniqueIndex`（M1 §4.5），非 `post_init_hook`。

| 字段 | 类型 | 说明 |
|---|---|---|
| `import_batch_id` | M2O → import.batch | 溯源批次 |
| `project_name` | Char | 工程名称 |
| `sub_division` | Char | 子分部 |
| `province` | Char | 地区 |
| `price_period` | **Date（视图 `options="{'max_precision':'months'}"` 月份选择）** | 价格期；Odoo 19 无 `fields.Month`、无 `widget="month"`，用 Date 字段 + `options` 限定月份精度（只选年月、不选日），存当月 1 日可排序/分组 |
| `ordinal` | Char | 序号（清单内行号） |
| `item_code` | Char(12), index | 项目编码（**支持9位或12位输入**；12位时后3位为自编顺序码。入库保留完整编码，匹配时取前9位） |
| `item_code_version` | Selection [`2013`/`2024`/`unknown`] | **【v3.3 新增】清单版本标识**。导入时由用户选择或自动识别；用于跨版本编码规则适配（2024版允许跨单项工程重码、新增总图工程 `ZXXX` 前缀等） |
| `item_name` | Char, index | 项目名称（原始） |
| `item_feature` | Text | 项目特征（多行） |
| `unit` | Char | 计量单位 |
| `quantity` / `quantity_num` | Char + Float(16,6) | 工程量（文本保真 + 数值排序） |
| `unit_rate` / `unit_rate_num` | Char + Float(16,6), nullable | 综合单价 |
| `total` / `total_num` | Char + Float(16,6), nullable | 合价 |
| `provisional_sum` / `provisional_sum_num` | Char + Float(16,6), nullable | 暂列金额/暂估价（视图中文标签） |
| `std_name` | Char | **标准化项目名称**（P0 新增，供无编码清单聚合） |
| `std_spec` | Char | **标准化规格**（P0 新增） |
| `material_dict_id` | M2O → material.dict | **关联三级材料分类字典**（P0 新增） |
| `parent_id` / `sequence` / `depth` | — | 层级 + 排序 |
| `source_path` | Char | 来源文件完整路径（**仅人读参考**，文件可能已被移动/删除；正式溯源走批次 `archive_path`） |
| `source_sheet` | Char | **【v6.1 新增】原始 Sheet 名**；与批次 `archive_path` + 本行 `sequence`（= 原始物理行号）三者合起来实现**单元格级溯源**：任意一条单价都能定位到「哪份归档文件 → 哪个 Sheet → 第几行」。这是 4.2「任何数字能回原始文件」目标的完整落地——v6 仅做到批次级，v6.1 补到行列级 |
| `classification` / `extra` | Json | 编码解析专业/分部；未映射原表列 |
| `version` | Integer | 业务层版本追踪（并发复用原生） |
| **`data_source_type`** | **Selection, `required=True`、无默认值** | **【v6 → v6.5 强化】数据性质**：`completed` 已完工程 / `control_price` 控制价 / `bid_price` 投标报价 / `pending_review` 待审 / `info_price` 信息价。**模型层必填、无默认**——任何写入都必须显式带值，防污染硬边界在模型层（详见 4.8）。**统计默认只取 `completed`** |
| **`match_key`** | Char, index | **【v6】行级候选归类键**（计算字段）：见 7.5。**跨行历史价聚合用 `aggregate_id`，不直接用 `match_key`** |
| **`match_key_source`** | Selection | `match_key` 的生成来源（`dict`/`std`/`code`/`raw`），让统计口径**可解释** |
| **`aggregate_id`** | Char, index | **【v6.5 新增】跨行聚合身份**：`material_dict_id` 存在时 = `dict:<id>`，否则 = `match_key`。M3 单价分析 / M4 成本库迁移**一律按 `aggregate_id` 聚合**，确保同一物料的不同标注状态行并为一族，避免历史价分裂成重复条目（详见 M1 §4.7 / 7.5） |
| **`unit_std`** | Char | **【v6】归一化单位**（米/m/米(m) → `m`；千克/kg → `kg`）。跨工程聚合的前置条件 |
| **`active`** | Boolean, 默认 True | **【v6】软删除标记**，不再物理删除（详见 4.7） |
| **`orphaned`** | Boolean, 默认 False | **【v6.2 补定义】重导产生的"孤儿行"标记**（原批次有、新导入无行 → 不自动删，进待确认列表）。**`active=False` 且 `orphaned=True`** 统一进入回收站/待确认，不另建状态。`anomaly_flag` 只表达数据质量，与删除状态正交（详见 4.7 状态语义） |
| **`biz_id`** | Char(36), unique index | **【v6.6 新增 · 稳定业务 ID】** UUID，create 时生成、**不可变**。未来智能体/API 一律用 `biz_id` 作长期接口标识，**禁止用 Odoo 自增 `id`（res_id）**——重装/迁移/分库会使 res_id 失效（详见 §19.5）。内部 ORM 仍用 `id` |

**索引**：`item_code`、`item_code+unit`、`project_name+sequence`、`item_name`、`province+price_period`、`material_dict_id`、`parent_id`、**`match_key`**、**`data_source_type`（含于复合索引）**、`active`。`source_sheet` 一般无需单列索引（经 `import_batch_id` + `sequence` 即可定位）；若溯源查询频繁可酌情加 `(import_batch_id, source_sheet, sequence)` 复合索引。

### 7.2 `zaojia.import.batch`（导入批次）

基础字段：`name` / `source_file` / `file_hash`(index) / `row_count` / `imported_count` / `skipped_count` / `anomaly_count` / `imported_at` / `province` / `price_period` / `operator`。

**【v6 新增】**

| 字段 | 类型 | 说明 |
|---|---|---|
| `archive_path` | Char(1024) | **原始文件归档相对路径**（4.4）；归档失败则整批中止，一行不写 |
| `source_path` | Char(1024) | 原始路径，**仅作人读参考**（文件可能已被移动/删除） |
| `checksum_count` | Integer | 入库行数校验（4.11） |
| `checksum_total` | Float(16,2) | `sum(total_num)`，与 Excel 合计行比对 |
| `checksum_hash` | Char(64) | 全行内容 SHA256，检测任何静默改动 |
| `data_source_type` | Selection | 批次级数据性质（行级可覆盖），默认 `completed`（4.8） |
| `active` | Boolean, 默认 True | 软删除标记（4.7） |
| `biz_id` | Char(36), unique index | **【v6.6 新增 · 稳定业务 ID】** UUID，不可变；未来 API 的 `batch_id` / `source_file_id` 语义载体（详见 §19.5） |

> **幂等（v6 修正 · 重要）**：同 `file_hash` 重导时**不再「先删旧批次全部 boq_item」**，改为 **4.6 的分层 Upsert** —— A 类字段更新、B 类（人工标注）字段永不覆盖、孤儿行进待确认列表。**这是 v5 中最危险的一处设计，已推翻。**

### 7.3 `zaojia.material.dict`（三级材料分类字典）【P0 · 风险1】

**为什么必须**：大量老清单无国标编码，仅靠 `item_name + item_feature` 无法可靠聚合，需业务字典做标准化。

| 字段 | 类型 | 说明 |
|---|---|---|
| `level` | Selection L1/L2/L3 | 层级 |
| `parent_id` | M2O self | 树形三级（大类→系列→规格集合） |
| `name` | Char | 名称（L3 为规格集合名） |
| `cat_l1` / `cat_l2` / `cat_l3` | Char | 冗余平铺，便于检索/聚合，避免递归 |
| `synonyms` | Json/Text | **物料业务同义词**（如 电缆/电力电缆/YJV） |
| `spec_whitelist` | Json/Text | 规格白名单集合（合法规格取值，用于归一/校验） |
| `biz_id` | Char(36), unique index | **【v6.6 新增 · 稳定业务 ID】** UUID，不可变；未来 API 的 `material_id` 语义载体（详见 §19.5） |

**职责边界（重要）**：`aliases.py` = Excel **表头列名**别名映射；`material.dict.synonyms` = **物料业务同义词**。二者不可混淆。字典提供 tree/form 视图，UI 可编辑维护，不只在 py 文件里。

### 7.4 `zaojia.cost.catalog`（评估 · M3 后）【风险9 方案B】

MVP 用**方案 A**：直接从 `boq_item` 按 `aggregate_id` 聚合作为历史单价库（标准化字段已就绪），**不建独立表**。若规模假设被突破（见 §4.14）或实时聚合不可接受，再新增 `cost.catalog`——但**它明确界定为「派生自 PG 的只读预聚合缓存（物化视图等价物）」：不接收任何直接写、可随时从 `boq_item` 重建，因此不构成 §4.1 所反对的「第二真相源」**。它与真相源的关键区别：① 无独立写入路径（只由迁移/刷新从 PG 重算）；② 漂移时以 PG 为准重新生成，而非人工维护。详见 M4 §3。

> **【v6.6 新增】** 若建 `cost.catalog`，其模型须含 `biz_id`（Char(36), unique index，UUID 不可变），作为未来 API 的 `cost_catalog_id` 语义载体（见 §19.5）。

### 7.5 `match_key`：把「什么算同一个清单项」变成一等公民【v6 新增 · P1】

**第一性原理追问**：本产品唯一的核心价值是回答「这个清单项我们以前做过多少钱」。那么「**什么算同一个清单项**」就是这个系统最核心的概念——但 v5 里它散落在三处（`item_code+unit` / `std_*` / `material_dict_id`），没有任何地方说清优先级。M3 做单价分析时必然出现"到底按哪个键聚合"的混乱。

**解法：引入统一计算字段 `match_key` + 来源标记 `match_key_source`，按固定优先级生成。**

| 优先级 | 条件 | `match_key` 形态 | `match_key_source` |
|---|---|---|---|
| 1 | 有 `material_dict_id` | `dict:<dict_id>\|<unit_std>` | `dict` |
| 2 | 有 `std_name` + `std_spec` | `std:<std_name>\|<std_spec>\|<unit_std>` | `std` |
| 3 | 有 9 位合规 `item_code`（或 12 位截取前9位） | `code:<code_9>\|<unit_std>` | `code` |
| 4 | 兜底 | `raw:<归一化名称>\|<归一化规格>\|<unit_std>` | `raw` |

> **【v6.5 澄清 · 身份 vs 键】** `match_key` 是**行级候选归类键**——它按该行「当前已填字段」生成，会随人工标准化升级而从 `raw:` → `std:` → `dict:`。**它不该被直接当作跨行聚合（join）键**：同一物理物料的 A 行（已标准化→`std:`）与 B 行（未标准化→`code:`/`raw:`）会因键型不同而**分裂成多条历史价**，而非一条可解释条目。真正的跨行聚合身份是 **`aggregate_id`**（M1 §4.7 定义）：一旦某行挂上 `material_dict_id`，它本身及其所有同类（无论各自 `match_key` 是 `std/code/raw`）都并入同一 `dict:` 族。**M3 单价分析、M4 成本库迁移一律以 `aggregate_id` 聚合**，仅用 `match_key_source` 做口径解释。

> **注意**：`is_valid_gb_code()` 校验9位纯数字编码；系统同时支持12位输入（含自编顺序码），入库时保留完整12位用于溯源，匹配时取前9位。详见 M1 §4.3。

**国标 9 位编码结构**：

```
┌──────────┬───────────┬──────────┬──────────┐
│ 第1-2位   │ 第3-4位    │ 第5-6位   │ 第7-9位   │
│ 专业工程  │ 附录分类  │ 分部工程 │ 分项工程  │
│ 顺序码   │ 顺序码    │ 顺序码   │ 顺序码    │
│ (统一)   │ (统一)   │ (统一)   │ (统一)   │
└──────────┴───────────┴──────────┴──────────┘
示例: 01 01 01 001  →  房屋建筑·土石方·土方·平整场地
      ↑专业           ↑附录    ↑分部    ↑分项
```

| 专业码 | 专业名称 |
|---|---|
| `01` | 房屋建筑与装饰工程 |
| `03` | 通用安装工程 |
| `04` | 市政工程 |
| `05` | 园林绿化工程 |

**同时补一个 v5 遗漏的基础问题：单位归一化（`unit_std`）。** 同一根电缆，A 工程写 `m`、B 写 `米`、C 写 `米(m)`，直接按 `unit` 聚合会被拆成三组，历史单价统计直接失效。归一规则（放在 `aliases.py` 的独立段落，**与表头别名分开**）：

| 标准值 | 接受的输入 |
|---|---|
| `m` | 米 / m / M / 米(m) / 延米 |
| `kg` | 千克 / kg / KG / 公斤 |
| `t` | 吨 / t / T |
| `台` / `套` / `个` / `组` / `㎡` / `m³` … | 同义与全半角变体 |
| 无法识别 | 原样小写去空格，**并标 warning**（提示补充映射） |

> **落地价值**：M3 的"同类项历史单价统计"直接按 `match_key` 聚合即可，且界面上可显示"本次统计中 60% 来自编码匹配、30% 来自标准化、10% 来自原始名称"——**统计口径可解释**，这是审计场景的硬要求。

### 7.6 `zaojia.audit.log`（不可变审计日志）【v6 新增 · P1】

字段与规则见 **4.10**。落库要点：

- 独立模型，`write()` / `unlink()` **直接抛异常**（append-only）；
- 不出现在常规菜单里，仅「数据管理 → 审计日志」只读列表，无新建/编辑按钮；
- `boq_item` 的 `write()` 覆写中逐字段比对，仅对**实际变化**的字段写日志，避免日志爆炸；
- 导入与导出动作在**服务层**显式写日志（不走 ORM write 覆写）。

## 八、模块结构

```
zaojia_boq/
  __manifest__.py
  models/
    boq_item.py               # 含 match_key 计算、write() 覆写写审计日志
    import_batch.py           # 含校验和、软删除
    material_dict.py          # 新增 P0
    audit_log.py              # 【v6】不可变审计日志（write/unlink 抛异常）
  wizards/
    import_wizard.py          # 导入：预览不写库 → 确认入库（含归档 + 分层 upsert）
    import_wizard_views.xml
    material_match_wizard.py  # 新增 P1：rapidfuzz 候选匹配
    material_match_wizard_views.xml
  security/ir.model.access.csv
  views/
    boq_item_views.xml
    import_batch_views.xml
    material_dict_views.xml   # 新增 P0
    audit_log_views.xml       # 【v6】只读审计日志列表
    analysis_views.xml
    menu.xml
  static/                     # 新增 v5：深空科技方案C呈现层
    src/scss/
      primary_variables.scss  # 挂 web._assets_primary_variables：主色/圆角/字号
      tokens.scss             # 设计 token（CSS 变量），浅色默认值
      dark_tokens.scss        # 深色模式取值（挂 web.assets_backend_lazy_dark）
      components/
        _navbar.scss
        _list.scss
        _form.scss
        _search.scss
        _buttons.scss
        _kpi_card.scss
        _wizard_steps.scss
        _empty_state.scss
    src/js/
      kpi_card.js             # OWL 组件：单价分析页 KPI 卡片（可选）
    description/icon.png
  services/                  # 【v6.6 新增】独立 Service 层（UI 无关，Odoo 向导/Controller 与未来 Agent 共用）；详见 §19
    import_service.py         # 导入解析（**只读打开源文件**，见 4.3）/ 预览 / 确认入库
    upsert_service.py         # 分层 Upsert 入库（§4.9）
    archive_service.py        # 原始文件归档与完整性校验（4.4）
    boq_query_service.py      # 查询/检索（search / get）
    price_service.py          # 历史单价查询 / 单价分析
    material_match_service.py # 物料匹配召回 / 确认回填
    batch_service.py          # 批次列表 / 详情
    evidence_service.py       # 来源溯源（source_location）
    data_quality_service.py   # 数据质量状态
  data/
    aliases.py                # 仅 Excel 表头列名别名 +【v6】单位归一映射（分两个字典，勿混）
    unit_normalize.py         # 【v6】单位归一化（7.5）
  tests/
    test_import.py            # 真实国内 Excel 样例（合并单元格/空单价/汇总行）
    test_aliases.py           # pytest
    test_material_dict.py
    test_security.py          # 【v6】S1/S2/S3/S4/S5/S7 安全验收用例
    test_archive.py           # 【v6】归档与校验和 S2/S6
```

## 九、前端呈现层：深空科技方案C落地（v5 新增）

### 9.1 设计原则（先立规矩，避免跑偏）

**核心取舍：取深空科技的深色驾驶舱风格，但适配造价数据的**高密度信息需求**。**

深空科技方案是深色数据驾驶舱，适合长时间监控（section 间距 80–120px、一屏一个卖点）；Odoo 后端是高密度数据工作台（一屏要看十几行清单、几十个字段）。照抄留白会直接毁掉可用性。因此：

| 维度 | 原设计原样 | 本项目取值 | 理由 |
|---|---|---|---|
| 色彩 / 中性灰阶 | 直接采用 | **全量采用** | 与信息量无关，纯观感提升 |
| 字体 / 字阶 / 字距 | 直接采用 | **全量采用**（字号整体上浮 1–2px） | Odoo 默认 13px 偏小，中文字形更易糊 |
| 圆角 / 阴影 / 分隔线 | 直接采用 | **全量采用** | 同上 |
| 动效曲线与时长 | 直接采用 | **全量采用**（时长砍半） | 官网 600ms 的缓慢动效在工作台里显拖沓 |
| **留白节奏** | 80–120px | **按 0.3 倍压缩 → 24–32px** | 保证信息密度 |
| **列表行高** | 不适用 | **≥40px（不低于 Odoo 默认）** | 一屏可见 ≥15 行是硬指标 |
| 毛玻璃 | 导航栏重度使用 | **仅顶部导航 + 弹层遮罩** | 表格行上用 `backdrop-filter` 会掉帧 |

### 9.2 设计 Token（单一事实来源）

全部定义为 CSS 变量，写在 `static/src/scss/tokens.scss`，组件样式**只允许引用变量、禁止裸值**（便于换肤与回归）。

| 类别 | Token | 浅色 | 深色 |
|---|---|---|---|
| 背景 | `--zj-bg` | `#0B0F1A` | `#0B0F1A` |
| 次级背景 | `--zj-bg-subtle` | `#121829` | `#121829` |
| 悬浮/选中 | `--zj-bg-hover` | `rgba(255,255,255,.06)` | `rgba(255,255,255,.08)` |
| 主文本 | `--zj-text` | `#E8ECF6` | `#E8ECF6` |
| 次级文本 | `--zj-text-2` | `#96A0BC` | `#96A0BC` |
| 三级文本 | `--zj-text-3` | `#6B7689` | `#6B7689` |
| 分隔线 | `--zj-separator` | `#232C40` | `#2A3550` |
| 主色（深空蓝） | `--zj-accent` | `#4D7CFE` | `#4D7CFE` |
| 主色悬浮 | `--zj-accent-hover` | `#6B91FF` | `#6B91FF` |
| 辅色（青） | `--zj-accent-2` | `#22D3EE` | `#22D3EE` |
| 成功 | `--zj-success` | `#34D399` | `#34D399` |
| 警告 | `--zj-warning` | `#FBBF24` | `#FBBF24` |
| 错误 / 异常 | `--zj-danger` | `#F87171` | `#F87171` |

> **颜色语义约定**：本工具非行情类界面，**红色统一表示「异常 / 错误 / 需处理」**（与 工业软件常见语义一致），不套用证券市场涨跌配色；绿色表示「正常 / 通过」。

**字阶与排版**

| Token | 浅/深色 | 取值 |
|---|---|---|
| `--zj-font-family` | 同 | 系统字体栈，见 9.4 |
| `--zj-fs-display` | 同 | 40px / 600 / `-0.02em` |
| `--zj-fs-h1` | 同 | 28px / 600 / `-0.015em` |
| `--zj-fs-h2` | 同 | 22px / 600 |
| `--zj-fs-h3` | 同 | 17px / 600 |
| `--zj-fs-body` | 同 | **14px** / 行高 1.5 / `-0.01em` |
| `--zj-fs-caption` | 同 | 12px / `--zj-text-3` |

**其他**

| Token | 取值 |
|---|---|
| `--zj-radius-sm` / `md` / `lg` / `pill` | 8 / 12 / 18 / 980 px |
| `--zj-shadow-1` | `0 1px 2px rgba(0,0,0,.05), 0 1px 8px rgba(0,0,0,.04)` |
| `--zj-shadow-2` | `0 4px 16px rgba(0,0,0,.08)` |
| `--zj-space-section` | 24px（列表/表单区），32px（页面级分组） |
| `--zj-dur-fast` / `--zj-dur` | 160ms / 280ms |
| `--zj-ease` | `cubic-bezier(.25,.1,.25,1)` |

### 9.3 落地路径（CSS 变量 + Jinja2 模板）

三条铁律：**① 所有颜色走 CSS 变量，组件内不写裸色值；② 不复制参考案例的商业资产；③ 设计 token 集中定义，深浅色双套值。**

1. **CSS 变量定义**：`app/static/css/tokens.css` 定义 `:root` 与 `[data-theme="dark"]` 两套变量值，组件样式零改动即可切换深浅色。
2. **组件样式**：`app/static/css/components.css` 做观感层定制，引用 token 变量，不写裸色值。
3. **Jinja2 模板**：`app/templates/` 目录下各页面模板继承 `base.html`，统一引入 tokens.css + components.css。
4. **前端 demo 基线**：`app/static/demo/index.html`（从 Odoo 版原样复用）作为像素级视觉规格，真实页面逐场景复刻。
5. **深色模式**：通过 `<html data-theme="dark">` 切换，跟随系统设置或用户偏好。

**验证无裸色值的命令**（每次改完 CSS 跑）：

```bash
grep -rn "#[0-9a-fA-F]\{3,6\}" app/static/css/components.css  # 组件样式不得出现裸色值（tokens.css 除外）
```

### 9.4 字体：系统字体栈，不内嵌

深空科技风格强调深色背景上的发光强调色与多色图标对比。但**不内嵌字体文件**（体积大、许可复杂），改用系统字体栈自动降级：

```css
--zj-font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text", "SF Pro Display",
                  "PingFang SC", "Helvetica Neue", "Microsoft YaHei", "Segoe UI",
                  Roboto, sans-serif;
```

| 运行环境 | 实际命中 | 观感 |
|---|---|---|
| macOS | SF Pro + 苹方 | **100% 苹果原生风格** |
| iOS/iPadOS | 同上 | 同上 |
| **Windows（本项目主环境）** | 降级到 **微软雅黑 / Segoe UI** | **约 80% 接近**：字形骨架不同，但字号/字重/字距/留白节奏一致，整体协调 |
| Linux | 降级到系统无衬线 | 同上 |

> **诚实提示（写进产品文档）：本项目主用环境是 Windows，字形会降级。** 追求 深空科技观感应优先保证排版节奏与色彩层级，不要指望字形完全一致；若未来确实需要，可评估引入开源替代字体（如 Inter / 思源黑体），但需单独确认许可与加载性能。

### 9.5 组件规范（Jinja2 模板 + CSS）

| 组件 | 深空科技化（深色底 + 发光强调色） | 实现方式 |
|---|---|---|
| **顶部导航** | 半透明毛玻璃 `backdrop-filter: saturate(180%) blur(20px)`、高度 48px、底部 1px 分隔线 | base.html 顶栏 + CSS |
| **应用菜单 / 首页** | 图标圆角矩形、卡片式入口、间距 24px | Jinja2 模板 + CSS |
| **列表视图** | 去斑马纹、行高 ≥40px、悬浮轻高亮、圆角 12px 容器 + 极轻阴影、**表头次级文字色 + 12px** | Jinja2 循环 + CSS |
| **表单视图** | 分组卡片化（白底 + 圆角 + 1px 分隔）、字段标签 12px 次级色、输入框聚焦主色描边（不用粗边框） | Jinja2 表单 + CSS |
| **搜索视图** | 筛选条件改为**胶囊 chip**（可一键移除，配 `×`）、搜索框圆角胶囊 | Jinja2 + JS 交互 |
| **按钮** | 主操作：实心主色胶囊按钮；次操作：无边框文字按钮；危险操作：红色文字 | CSS class |
| **KPI 卡片**（单价分析页） | 大数字（28px/600）+ 次级说明文字 + 圆角 18px 卡片 + 轻阴影 | Jinja2 模板 + CSS |
| **导入向导** | 顶部**步骤条**（上传 → 解析 → 预览 → 确认）、预览表异常行用 `--zj-warning`/`--zj-danger` 背景与图标标记 | 多步表单 + Session + CSS |
| **异常单价标红** | 数值用 `--zj-danger` + 轻红底（`rgba(255,59,48,.08)`），兼顾色觉障碍（同时有图标/文字标记） | CSS class |
| **空状态** | 居中插图位（占位）+ 一句说明 + 主色引导按钮，不显示空表头 | Jinja2 模板 |
| **弹层/对话框** | 圆角 18px、标题 17px/600、遮罩毛玻璃 | 焦点管理与键盘操作 |

### 9.6 约束与风险

| 项 | 约束 | 原因 |
|---|---|---|
| 单文件 CSS 体积 | 编译后 < 120KB | 后端首屏加载 |
| `!important` | 仅用于覆盖第三方/内核高优先级规则，**逐处写注释说明原因** | 否则失控、升级即崩 |
| `backdrop-filter` | 仅导航栏与遮罩，禁用于表格行/单元格 | 大表滚动掉帧 |
| 动画 | 统一 `--zj-ease`，禁用 `transition: all` | 避免误伤与性能抖动 |
| 响应式 | 优先 1440 / 1280 桌面；平板只读查询；**手机端不优化** | 造价清单字段多，小屏无解 |
| Odoo 大版本升级 | 升级后必须重跑视觉基线（9.7），重点看被覆盖的选择器是否仍存在 | Odoo 每年一版，DOM 类名会变 |
| 卸载清洁性 | 卸载模块后观感回到 Odoo 默认 | 证明未污染核心 |

### 9.7 视觉验收（基线截图法，替代不了人工核对）

纯视觉改动**无自动化断言可用**，采用「基线截图 + 清单核对」：

在浅色与深色各截一套，共 8 张基线，存 `docs/visual_baseline/`：

1. 登录页　2. 应用菜单　3. 查询检索（列表 + 搜索 chip）　4. 清单表单详情
5. 单价分析（KPI 卡片 + pivot）　6. 导入向导（预览含异常行）　7. 物料字典（树形）　8. 深色模式列表页

**逐张核对清单**：主色为 `#4D7CFE`｜正文 14px｜行高 ≥40px｜一屏列表 ≥15 行｜圆角/阴影符合 token｜无裸值颜色｜无横向滚动条｜深色模式无白底残留｜中文无截断。

## 十、导入向导设计（P0 关键）【风险4】

明确流程：**（只读打开）→ 解析 → 预览 → 确认 →（归档）→ 写入**，**预览阶段绝不直接写库**。

0. **【v6】只读打开 + 归档**：`load_workbook(read_only=True, data_only=True)`；拒收 `.xlsm/.xlsb`；超限拒绝。用户点「执行入库」时**先复制归档、后写库**（4.3 / 4.4）。
1. **合并单元格**：openpyxl 读取时合并区域取左上角值填充，避免 None 串。
2. **汇总行识别**：匹配「合计/小计/分部合计/单位工程合计」等模式，标记为跳过，不入 `boq_item`。
3. **异常标记**：单价为空（允许，统计排除）、工程量为 0/负数、文本乱码 → 预览页标记 警告/错误（呈现见 9.5）。
4. **字段映射**：`aliases.py` 中文表头别名 → 规范字段；**单位另走 `unit_normalize.py` 归一化**（7.5）。
5. **【v6】数据性质确认**：向导上必须有 `data_source_type` 选择项，默认 `completed`；选 `pending_review`（待审）时**界面明确提示"本批数据不会被计入历史均价"**（4.8）。
6. **预览确认**：界面展示**新增 N / 更新 M / 孤儿待确认 K / 保留人工标注 X** 四项计数 + 跳过与异常原因，用户核对后点「执行入库」才写库（OCE 导入核心思想）。
7. **【v6】幂等 = 分层 Upsert**：**不再是"先删旧批次"**。A 类字段更新、B 类人工标注永不覆盖、孤儿行进待确认列表（4.6）。
8. **【v6】入库后固化校验和**：写 `checksum_count` / `checksum_total` / `checksum_hash`，并与 Excel 合计行自动比对，不一致即在结果页高亮（4.11）。

## 十一、相似度召回（rapidfuzz）【P1 · 风险2】

- 位置：`wizards/material_match_wizard.py`。
- 针对未标准化（`material_dict_id` 空）的 `boq_item`，调 rapidfuzz 从 `zaojia.material.dict` 按 `name+spec` 召回 Top-N 候选。
- **硬规则：仅候选建议，绝不自动改 `boq_item`；人工确认后回填 `std_name/std_spec/material_dict_id`**。

## 十二、向量语义检索（LanceDB）【P2 · 候选项 · 风险3】

> **【v6.5 降级】** 原设计把 LanceDB 作为默认能力详细设计；经第一性原理审查，降级为**候选项**，并加启动门槛。理由：① 它是为「PG 与 LanceDB 双源」这个**自造问题**再发明的一套分布式一致性协议（M4 §5.6 五机制），对单机单用户属过度设计；② 其增量价值未被证实——文档示例（「YJV 4*16」vs「YJV铜芯电力电缆 16平方」）用 `material.dict` 同义词（电缆/电力电缆/YJV）**在 rapidfuzz + 字典归一层面即可召回**；③ 若确需向量能力，对本地工具 **pgvector（直接进 PG）反而比「PG + 外部 LanceDB + 一致性协议」简单一个数量级**，应优先评估 pgvector。

- **启动门槛（须同时满足才启用）**：(a) rapidfuzz + `material.dict` 同义词归一在**真实数据**上的高价值项召回率不达标（如 < 90%）；(b) 数据规模确实需要（见 §4.14 规模假设）；(c) 已评估 pgvector 不可行。
- 在未达标前，匹配能力仅靠「精确国标编码 + rapidfuzz + 字典同义词」三层即可，不引入第二个存储。
- 若未来确证需要：LanceDB 作为**外部独立文件向量库**，不塞 PG；embedding 不存入 Odoo PG；业务层 python 调用 LanceDB 做语义召回，结果回灌匹配向导候选。
- **仅当启用时**才需要 M4 §5.6 的 PG↔LanceDB 一致性协议（写序 / 幂等 upsert / `vector_sync_pending` 补偿 / 孤儿向量清理 / `embedding_version` 漂移检测 / `reconcile_vectors()` 对账）。审计与统计口径一律以 PG 为准，向量检索结果仅作候选、须标注来源且可能存在同步延迟。

## 十三、并发与乐观锁【风险5】

复用 Odoo 原生并发（`write_date` + ORM 写锁）。`boq_item.version` 仅作业务层版本追踪字段，不重复实现乐观锁机制。

## 十四、导出【风险6】

复用 Odoo 列表视图 Excel/CSV 导出；明确支持**筛选后结果集**导出，包含原始字段（`item_name`/`item_feature`/…）+ 标准化字段（`std_name`/`std_spec`/`material_dict_id`）+ **溯源字段（`import_batch_id`/`archive_path`/`data_source_type`）**。

**【v6 补充 · 防外泄】**

| 规则 | 做法 |
|---|---|
| 导出留痕 | 每次导出写 `audit_log`（谁、何时、筛选条件、行数、导出字段）—— 单价外流时可回溯 |
| 大批量闸门 | 单次 > 1000 行需二次确认，并提示"本次导出含商业敏感单价" |
| 导出内容 | 默认不含 `checksum_*` 等内部字段；`archive_path` 默认带（便于溯源） |

## 十五、权限【风险7】

**FastAPI 适配**：三角色权限体系通过 FastAPI 依赖注入（`Depends(require_role(...))`）实现，替代 Odoo `ir.model.access.csv`。认证源为公司 OA 统一登录（易达 ECMS），开发期独立账号占位。

| 角色 | 权限 |
|---|---|
| **admin** | 导入/删除/标注/用户管理/系统设置；可硬删（须先导出快照） |
| **estimator** | 查询/导出/单价分析；B 类字段只读（无 reason 拒绝写入） |
| **viewer** | 只读浏览；**禁止导出** |

**【v6 补充 · 防外泄】**

| 对象 | 权限 |
|---|---|
| `audit_log` 表 | **所有角色仅读**（append-only，无 update/delete）；模型层亦直接抛异常，双保险 |
| 归档目录 | 走**操作系统文件权限**，不依赖应用权限（应用账号泄露也删不掉原始文件） |
| 硬删除 | 仅 admin；且须先自动导出快照（4.7），快照失败不得放行（Odoo 版 R1 缺陷教训） |
| 数据库 | 仅监听内网，不暴露公网；PG 账号最小权限 |

## 十六、备份策略【风险8 · P0】

> **总策略见 4.9（3-2-1 + 恢复演练）。本节仅列备份对象清单。**
> **【v6 更正】** 旧表述"DB 是唯一数据源，原文件丢失不可恢复"因果倒置 —— **原始 Excel 才是真相源，数据库是可重建的派生物**（4.1）。因此备份优先级为：**原始文件归档 > 人工标注 > 数据库**。

| 备份对象 | 方式 | 频率 |
|---|---|---|
| **原始 Excel 归档目录**（最高优先） | 归档即不可变副本；整体同步到异地盘 | 与导入同步 |
| **数据库**（含人工标注） | `pg_dump`（Windows 计划任务），异地份加密 | 每日，保留 30 天滚动 |
| `static/` 与 `docs/visual_baseline/` | 随模块进 Git，天然备份 | 随提交 |
| **禁止入仓** | `*.sql` / `*.dump` / `filestore/` / 真实工程 Excel（4.12） | — |

**恢复演练为强制项**：每季度一次，在临时库完整恢复并抽样 10 条与原始 Excel 逐字比对，结果记入 `docs/backup_drill.md`。**未演练过的备份视为无效。**

## 十七、部署

**FastAPI 适配**：开发期 uvicorn `127.0.0.1:8000`；生产期 Nginx 反向代理 + HTTPS + uvicorn worker，可作为公司网页子路径或子域名嵌入。PostgreSQL 15 独立部署（非 Windows 服务，需计划任务自启）。

| 环境 | 部署方式 | 说明 |
|---|---|---|
| 开发 | `uvicorn app.main:app --reload` | 本机 127.0.0.1:8000，热重载 |
| 生产 | Nginx + uvicorn (gunicorn worker) | HTTPS 终止、静态文件缓存、反向代理到 8000 |
| 数据库 | PostgreSQL 15（`E:\PostgreSQL\`） | 非 Windows 服务，`pg_ctl start` 手动或计划任务启动 |
| OA 对接 | 易达 ECMS SSO（待厂商确认协议） | 替换 `get_current_user`，不阻塞主体开发 |

> 原 Odoo 版部署（`E:\Odoo19\`，odoo-bin + PG）已归档，M2 完成前保持只读运行作为数据回滚兜底。

## 十八、里程碑

| 阶段 | 内容 | 完成标准 | 状态 |
|---|---|---|---|
| **M0 迁移准备** | FastAPI 骨架 + 纯函数迁移（720行）+ 90条测试全绿 + 前端 demo 复用 + Odoo 版归档（tag + pg_dump）+ 文档适配 | pure_tests 90 passed；FastAPI app 加载成功；demo 可访问；4份文档适配完成 | ✅ 完成（2026-09-05） |
| M1 数据模型 | SQLAlchemy 四模型 + biz_id + 编码解析 + 分层 Upsert + 审计落库 + 三角色权限 | 模型可 create_all；安全验收 S1–S9 通过；权限用例通过 | ⏸ 待启动 |
| **M1.5 设计系统** | token + 组件 CSS + 深浅色 + 导航/列表/表单/按钮（demo 已就绪，模板落地） | 8 项核对清单通过，8 张基线截图入库 | ⏸ 依赖 M1 |
| M2 导入与查询 | 导入向导（只读打开 / 归档 / 分层 Upsert / 校验和 / 合并单元格 / 汇总行 / 异常标记 / 预览不写库）+ 搜索 + 导出 | 真实清单导入→查询→导出闭环；S7、S8 通过，备份演练完成 | ⏸ 依赖 M1 |
| M3 单价分析 | KPI 卡片 + 仪表板（聚合/区间/异常，默认只用「已完工程」）+ 批次管理 + 回收站 + 匹配向导 P1 rapidfuzz + 评估 cost.catalog | 日常自用一周无阻塞；统计口径可解释 | ⏸ 依赖 M2 |
| OA SSO 对接 | 易达 ECMS 统一登录对接，替换 get_current_user | OA 登录可直接访问门户；三角色映射正确 | ⏸ 待厂商文档（独立线） |
| P2 | LanceDB 向量检索 + 数据迁移（Odoo→新库 ETL） | 语义检索可用；迁移后数据零丢失 | 📋 规划中 |

> **安全是 M1 的交付内容，不是事后补丁**：S1–S9（4.13）中除 S8（备份演练，需真实数据）外，**全部必须在 M1 结束前通过**。理由很简单——一旦库里存进了真实工程数据，再回头补软删除和审计日志，历史损失已经无法追回。

> **协作约定（2026-09-04 用户指令，永久生效）**：①总控（项目总控 Agent）只做记录与把控，**不得亲自修改实现代码**；②具体改动由执行 Agent 按泳道实施，**提交/推送权归总控，执行 Agent 一律不 commit**；③界面验收 = 与 app/static/demo/index.html **完全一致**（demo 即像素级视觉规格）；④文件单一所有权（共享文件 main.py/config.py/requirements.txt 归 Agent-0 集成守门统一收口）。详见 项目总控.md §3。

---

## 十九、智能体就绪架构（Service 层与 API 边界）【v6.6 新增】

> 来源：用户 2026-09-02 明确需求——系统未来须能接入**智能体（Agent）体系**，因此开发时**不要把核心能力绑死在 Odoo UI**；现在仍以「先把独立版本完整做出来、能实际使用」为第一目标，**不要求现在开发智能体接入**，只保证未来接入时**无需大规模重构**。

### 19.1 原则与定位

- **两条入口、同一套底层**：Odoo UI（当前独立系统 + 后续数据管理后台）与未来智能体（Agent），都调用**同一个独立 Service 层**；智能体**不去模拟浏览器点击 Odoo 页面**。
- **不冲突第一性原理**：Service 化、稳定 ID、API 边界，是对「原始 Excel 真相源 + 人工标注不可重建」第一原则的**工程落地强化**——把核心能力从 UI 抽离后，任何入口都走同一写路径 + 同一审计，反而更利于保护数据与可追溯。
- **当前阶段不做**：不实现对外 REST 服务、不接任何 Agent runtime、不做鉴权网关。仅要求 **Service 层真实落地 + API 协议与文档确定**。

### 19.2 分层结构

```
Odoo UI  ─┐
          ├─→ 独立 Service 层（Python，UI 无关）─→ 领域/数据处理逻辑 ─→ PostgreSQL / 文件归档 / 匹配 / 统计
未来 Agent ─┘                                                            （LanceDB 仅候选项 §十二）
```

- Service 层**不依赖** Odoo HTTP / 视图 / Wizard 上下文，可被 Odoo 向导、Odoo Controller、以及未来 REST Controller 共同调用。
- 核心业务逻辑（Excel 导入、解析、查询、匹配、统计、溯源、归档、标准化）**不得只存在于页面按钮、Wizard、Controller 或前端交互逻辑中**。

### 19.3 核心能力 → Service 模块映射（能力边界固定）

| 能力 | Service 模块（Python，M1–M4 落地） | 未来 REST 路径（v1，仅定协议） |
|---|---|---|
| 导入解析（只读预览） | `import_service.preview(file_ref)` | `POST /api/v1/import/preview` |
| 确认入库（写） | `import_service.commit(batch, options)` | `POST /api/v1/import/commit` |
| 查询清单项（读） | `boq_query_service.search(params)` | `POST /api/v1/boq/search` |
| 取单个清单项（读） | `boq_query_service.get(boq_item_id)` | `GET /api/v1/boq/{id}` |
| 历史单价查询（读） | `price_service.search_historical(params)` | `POST /api/v1/price/search` |
| 单价分析（读） | `price_service.get_analysis(params)` | `POST /api/v1/price/analyze` |
| 物料匹配召回（读） | `material_match_service.find_matches(params)` | `POST /api/v1/material/match` |
| 确认匹配（写） | `material_match_service.confirm_match(id, payload)` | `POST /api/v1/material/match/confirm` |
| 来源溯源（读） | `evidence_service.get_source(item_id)` | `GET /api/v1/evidence/{id}` |
| 批次列表（读） | `batch_service.list(params)` | `GET /api/v1/batches` |
| 批次详情（读） | `batch_service.get(batch_id)` | `GET /api/v1/batches/{id}` |
| 数据质量状态（读） | `data_quality_service.get_status(params)` | `GET /api/v1/quality/status` |

> 具体方法名/路径可微调，但**能力边界必须固定**。当前阶段 REST 路径仅作协议与文档确定，**不要求对外暴露**。

### 19.4 Service 层落地边界（M1–M4 须遵守）

1. Service 模块统一置于 `zaojia_boq/services/`（§八 目录已列），原 `data/` 下的 `import_service/archive_service/upsert_service` 视为 Service 层子集，实施时归入 `services/`。
2. Service 方法**只接受结构化参数（dict）、只返回结构化结果（dict/JSON 信封）**，**不返回** Odoo View 对象、HTML 或界面状态；Odoo 向导/视图负责把结构化结果渲染成 UI。
3. **读操作**（查询/统计/匹配候选）可直接调用；**写操作**（确认标准化 / 修改匹配 / 删除批次 / 正式导入 / 修改人工字段）**必须走独立写接口**，并带入审计元数据（§19.7）。
4. Service 调用领域逻辑（解析/归一/Upsert/聚合）；领域逻辑**不反向依赖** Service 层（依赖单向）。
5. 现有 `import_service` / `archive_service` / `upsert_service` / `analysis_service` / `cost_migration` / `vector_search_service` 视为 Service 层子集，统一纳入本节约束（不重复造）。

### 19.5 稳定业务 ID（未来接口标识依据）

> 【v6.6 新增】未来智能体调用时，**禁止把 Odoo 自增 `res_id` 当作唯一长期接口标识**（重装/迁移/分库会失效）。每个核心对象增加**稳定业务 ID `biz_id`（UUID，不可变）**：

| 对象 | 稳定 ID 字段（§7 已补） | 未来 API 语义 |
|---|---|---|
| 清单项 | `boq_item.biz_id` | `boq_item_id` |
| 批次 / 源文件 | `import_batch.biz_id` | `batch_id` / `source_file_id` |
| 物料字典项 | `material_dict.biz_id` | `material_id` |
| 成本库项（M4） | `cost_catalog.biz_id` | `cost_catalog_id` |

- 数据库内部 `id`（res_id）保留供 ORM 使用，但**未来外部协议一律用 `biz_id`**。
- **文件引用**支持三选一，避免只能经 Odoo 浏览器上传控件触发导入：`file_id`（= `import_batch.biz_id`）、`file_path`（受控内部归档路径 `archive_path`）、`file_hash`（SHA256，M1 已存在）。至少一种稳定方式。

### 19.6 结构化 JSON 接口契约（读/写分离 + 错误码）

所有 Service 返回统一信封：

```json
{
  "success": true,
  "data": [ ... ],
  "total": 128,
  "warnings": [],
  "trace_id": "uuid"
}
```

写操作失败统一错误码（机器可识别）：

```json
{
  "success": false,
  "error": { "code": "IMPORT_INVALID_FILE", "message": "文件格式不支持" }
}
```

- 错误码前缀约定：`IMPORT_` / `BOQ_` / `PRICE_` / `MATCH_` / `EVIDENCE_` / `BATCH_` / `QUALITY_`。
- 查询返回数据**必须携带来源定位对象**，便于智能体继续溯源（不只是一个统计数字）：

```json
"source_location": {
  "source_file_id": "<file_hash 或 batch.biz_id>",
  "import_batch_id": "<batch.biz_id>",
  "sheet_name": "<source_sheet>",
  "row_number": 12,
  "original_fields": { "item_code": "...", "item_name": "..." },
  "file_hash": "<sha256>"
}
```

> 来源定位字段（`import_batch_id` / `source_sheet` / `sequence` / `file_hash`）M1 已落库，本节约把"返回结构"固定为对外契约。

### 19.7 写操作的审计元数据（防手误 + 可追溯，承接 §4.10）

任何写接口必须携带并在 `audit_log` 记录：

| 字段 | 说明 |
|---|---|
| `operator` | 操作人（Odoo user 或未来 Agent 身份） |
| `reason` | 变更原因（B 类字段强制，见 §4.10 / §4.5） |
| `trace_id` | 本次调用链路 ID（与返回信封一致） |
| `timestamp` | 时间戳（自动） |

写操作进入现有 `audit_log`（§4.10），与人工界面操作同一条审计链——**智能体不是绕过审计的后门**。

### 19.8 API 版本化

- 统一 `/api/v1/...`，结构化 JSON 契约从第一版固定。
- 后续破坏性修改升 `/api/v2/`，**不改动 v1** 使未来调用方失效。
- 当前阶段：v1 路径与 JSON Schema 以本文档 §19 为权威记录；不要求部署对外服务。

### 19.9 当前阶段交付要求（不阻断独立版本开发）

M1–M4 开发过程中须满足：

1. 核心逻辑已 Service 化（§19.3 / §19.4）；
2. 接口输入/输出不依赖 UI（§19.6 结构化）；
3. 稳定业务 ID 已预留（§19.5）；
4. API v1 路径与 JSON Schema 有文档（本 §19）；
5. 至少为核心查询/分析/来源追溯提供**可调用测试**（pytest 直接调 Service，验证不依赖 UI）；
6. 未来接入智能体**无需重写核心业务逻辑**。
7. **就绪度量（可验证，PM 评审 2026-09-02 补充）**——对应里程碑结束时逐条核对，任一不满足则该里程碑不得标记完成：
   - §19.3 的 12 项能力 **100% 有 Service 方法与直调测试**（M1 交付 import/upsert/archive/query/batch/evidence 六项，M2/M3 补齐 price/match/quality 其余各项）；
   - 所有 Service 返回 **100% 结构化信封**（§19.6），无 Odoo View/HTML 泄漏到返回值；
   - 所有写路径 **100% 携带审计四元组**（§19.7：operator/reason/trace_id/timestamp）并落 `audit_log`；
   - 最小契约测试在 M2/M3 验收清单中**逐一点名接口**（M2 §9 T10=3 个读接口 / T11=1 个写链路，M3 §9.1 同理），不使用"附带测试"这类不可验收的表述。

> **不要求**：实现对外 REST 服务、接入任何 Agent runtime、实现鉴权网关——那些是未来接入阶段的事。
