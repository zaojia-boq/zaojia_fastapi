# 项目总控 · 造价数据门户（FastAPI 版）（唯一事实源）

> 版本：v1.0（2026-09-05 初版：从 Odoo 19 迁移至 FastAPI 的新项目总控，继承原项目业务契约与数据安全铁律，技术基线全面切换）
> 建立日期：2026-09-05 · 维护人：总控 Agent
> 原 Odoo 版总控：`E:\DEEPSEEK学习\造价数据库\项目总控.md`（v2.15，已归档，不再更新）
> 原 Odoo 版代码：git tag `legacy-odoo-final`（commit a93d82c），数据库备份 `E:\DEEPSEEK学习\zaojia_backup\zaojia_db_20260905.dump`

> **本文档是项目目标、架构、分工、任务分解、接口契约、变更决策的唯一事实源。**
> 子 Agent 的一切产出以本文档 + 各设计稿为准；冲突时以本文档的「契约裁决」为准。
> 未经过总控 Agent 确认并回写本文档，任何架构、表结构、接口改动不允许生效。

---

## 1. 项目目标

面向造价从业人员的**工程造价数据门户**：把多省多批次的工程量清单 Excel 安全导入、标准化、聚合成可分析的数据资产，最终支撑单价分析、成本库与语义检索。作为公司网页的一部分，支持团队共享查询。

**核心底线（继承自 Odoo 版，不可变）**：原始造价 Excel 与人工标注（B 类字段）不可重建、不可静默丢失。

**与 Odoo 版的关系**：本项目是 Odoo 19 版（`zaojia_boq` 模块，M0–M3 已完成）的平台迁移重写。业务契约、纯函数算法、数据模型设计 100% 继承；Odoo 框架代码（ORM/views/XML/OWL/SCSS/security CSV）全部重写为 FastAPI + SQLAlchemy + 模板实现。

## 2. 技术架构基线（不可变决策，改动须走变更提案）

| 项 | 决策 |
|---|---|
| 技术栈 | FastAPI + SQLAlchemy 2.0 + PostgreSQL 15 + Jinja2 模板（前端渐进可升级 Vue/React） |
| Python | 系统 Python 3.12（`C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe`） |
| 核心模型 | `boq_item` / `import_batch` / `material_dict` / `audit_log`（字段设计继承 Odoo 版，表名去掉 `zaojia_boq_` 前缀） |
| 数据分层 | A 类可重建（导入覆盖）、B 类不可重建（禁删禁静默覆盖）、C 类系统——**与 Odoo 版完全一致** |
| 聚合键 | `match_key` 优先级 dict > std > Z前缀 > 有效国标码 > raw，显式带版本维度；跨行聚合身份 `aggregate_id`（继承 ADR-001） |
| 存储约定 | `price_period` 存当月 1 日；`item_code` 按文本处理（9 位合规 / 12 位截前 9 / 10 位异常映射） |
| 稳定 ID | `biz_id` Char(36) UUID 不可变，API 一律用 `biz_id` |
| 接口信封 | `{success, data, total, warnings, trace_id}`；错误码前缀 `IMPORT_/BOQ_/PRICE_/MATCH_/EVIDENCE_/BATCH_/QUALITY_`（继承 Odoo 版契约） |
| 审计四元组 | 写操作必带 `operator / reason / trace_id / timestamp` 并落 `audit_log`（append-only） |
| 认证 | 公司 OA 统一登录（易达 ECMS，待厂商确认 SSO 协议）；开发期占位独立账号，OA 对接到位后替换 `get_current_user` |
| 角色权限 | 三角色：`admin`（导入/删除/标注/用户管理）/ `estimator`（查询/导出/单价分析，B 类字段只读）/ `viewer`（只读浏览，禁导出） |
| 纯函数层 | `data/` 目录纯函数（gb_code/aliases/unit_normalize/match_score/price_calc/quality_metrics/cost_catalog_gate），**从 Odoo 版原样复制，零改动**，不依赖任何框架；**2026-09-07 新增**：`tfidf_matcher.py`（TF-IDF 语义匹配）、`faiss_matcher.py`（FAISS 向量检索）、`learning_engine.py`（学习引擎权重自适应） |
| 外部检索 | LanceDB 向量检索独立于 PG，不侵入；rapidfuzz 仅候选建议、人工确认才回填；**2026-09-07 新增三算法融合**：rapidfuzz（精确匹配）+ TF-IDF（语义匹配）+ FAISS（向量检索），权重由学习引擎根据人工确认结果自适应调整 |
| 界面 | 深空科技设计系统（主色 #4D7CFE / 辅色 #22D3EE / 底 #0B0F1A），视觉规格继承 Odoo 版 demo/style-lab，实现重写 |
| 部署 | 开发期 uvicorn `127.0.0.1:8000`；生产期 Nginx 反向代理 + HTTPS，作为公司网页子路径或子域名 |

**从 Odoo 版吸取的教训（本项目强制遵守）**：
1. **测试先行**：安全铁律和核心业务先写自动化用例，再让 AI 写实现；
2. **小步 commit**：一个功能一 commit 一测试，不攒大改（Odoo 版 R1/H3 缺陷均为攒改漏测）；
3. **安全用例优先级最高**：S1–S9 任一失败 = 整体不通过；
4. **避免框架专有坑**：FastAPI/SQLAlchemy 的版本差异、异步陷阱、迁移回滚必须实测，不凭印象；
5. **视觉基线必做**：UI 改动后必须截图对照，不接受"看起来差不多"；
6. **敏感文件不入仓**：xlsx/xls/sql/dump 禁止进 git。

**契约裁决（pytest 范围）**：`pytest` 分两类——`pure_tests/`（纯函数，系统 Python 直接跑）与 `tests/`（FastAPI 集成测试，含 TestClient + 测试数据库），禁止混跑。

## 3. 组织与分工

### 3.1 总控 Agent（本角色）

- 维护本文档；拆解需求、分配子任务；评审子 Agent 产出（§5 评审清单）；
- 受理/裁决变更提案（§6）；维护版本进度（§4 状态列）；**不写大量业务实现代码**；
- 重要的设计决策全部回写本文档，不留在对话上下文。

### 3.2 总控角色铁律（2026-09-04 用户指令，永久生效，继承自 Odoo 版）

总控**只做记录与把控**——维护本文档、状态回写、阶段划分、门禁定义、验收核对与调度派工；**不得亲自修改实现代码**。具体改动（Python/SQLAlchemy/模板/配置）一律由执行 Agent 按阶段（泳道）实施，每阶段独立交付（改动 + 门禁 + commit）。违反即流程违规、交付无效。

### 3.3 子 Agent 编制（命名空间 = 任务编号）

| 子 Agent | 授权范围（可触碰） | 禁止触碰 |
|---|---|---|
| **M1-数据模型** | `app/models/`、`app/core/audit.py`（落库部分）、`tests/test_models.py`、数据库迁移脚本 | services/、api/、模板、data/ 纯函数 |
| **M1-权限认证** | `app/core/security.py`、`app/config.py`（OA SSO 参数）、`tests/test_security.py` | 业务模型、服务、data/ |
| **M2-导入服务** | `app/services/import_service.py`、`app/services/upsert_service.py`、`app/services/archive_service.py`、`tests/test_import.py` | models 字段定义、data/ 纯函数、其他 service |
| **M2-查询导出** | `app/services/query_service.py`、`app/services/batch_service.py`、`app/api/query.py`、`tests/test_query.py` | 导入逻辑、模型字段 |
| **M2-前端页面** | `app/templates/`、`app/static/`（CSS/JS）、`app/api/pages.py` | 服务层逻辑、模型 |
| **M3-单价分析** | `app/services/price_service.py`、`app/services/material_match_service.py`、`app/services/data_quality_service.py`、`tests/test_m3.py`；纯函数 `data/price_calc.py` 等仅调用不修改 | data/ 纯函数（只读调用）、M1/M2 已验收代码 |
| **M4-成本库检索** | `app/models/cost_catalog.py`、`app/services/vector_search_service.py`、LanceDB 集成脚本 | PG 核心表结构、data/ |
| **OA-SSO对接** | `app/core/security.py`（`get_current_user` 替换）、`app/config.py`（OA 参数）、对接文档 | 业务逻辑、模型、其他服务 |
| **Agent-0（集成守门）** | `app/main.py`（路由注册）、`app/__init__.py`、`requirements.txt`、配置文件 | 业务实现代码；交付总控 commit |
| **Lane-视觉基线** | `docs/visual_baseline/`（截图） | 共享文件；不 commit |

> 越权修改其他模块代码 = 评审直接不通过，回滚其改动。

### 3.4 子 Agent 输出规范（强制，不满足 = 评审不通过）

每个子 Agent 完成（或阶段性汇报）时，输出必须包含以下六项，缺一不可：

1. **改动文件清单**：精确到文件 + 改动性质（新增/修改/删除）；
2. **代码/测试产出**：测试随代码走，新逻辑必有对应测试（pure_tests/ 或 FastAPI 集成）；
3. **自验证据**：§9 登记命令的**原样执行记录**（命令行 + 结果摘要，如 `90 passed`），禁止"应该能过"式表述；
4. **契约对照表**：逐条对照本表授权范围与对应设计稿章节，声明"符合/偏差"；
5. **偏差与提案**：与设计稿有任何偏差 → 明确列出并附【变更提案】（动机/影响面/替代方案/迁移成本），**不得先斩后奏**；
6. **引用核对声明**：所引用的设计稿章节号须核对原文（B 类字段、聚合键、存储约定等）。

## 4. 全量任务分解（WBS）

> 状态：✅ 完成 · 🔄 进行中 · ⏸ 未启动（等依赖）· 📋 设计完成待实施 · 🧊 远期

### 4.1 已完成（M0 迁移准备）

| 任务 | 产出 | 验收 | 状态 |
|---|---|---|---|
| **M0.1 Odoo 版归档** | git tag `legacy-odoo-final`（commit a93d82c）+ pg_dump 备份 `zaojia_db_20260905.dump`（4.2MB，8444 TOC 条目） | tag 存在、备份可 pg_restore 读取 | ✅（2026-09-05） |
| **M0.2 FastAPI 骨架** | 项目目录 `zaojia_fastapi/`，含 app/（main/config/core/models/services/api）、data/、pure_tests/、requirements.txt、README.md | `from app.main import app` 加载成功，4 个路由注册（/health、/api/me、/api/public/hello、/api/admin/audit-test） | ✅（2026-09-05） |
| **M0.3 纯函数迁移** | `data/` 8 文件 720 行从 Odoo 版原样复制（aliases/gb_code/unit_normalize/match_score/price_calc/quality_metrics/cost_catalog_gate） | 文件逐字节一致（复制校验） | ✅（2026-09-05） |
| **M0.4 纯函数测试验证** | `pure_tests/` 7 文件从 Odoo 版复制并修正路径（`zaojia_boq/data` → `data`） | **90 passed in 0.25s**，零失败 | ✅（2026-09-05） |
| **M0.5 依赖安装** | fastapi 0.141.1 / uvicorn 0.52.4 / sqlalchemy 2.0.52 / pydantic 2.13.4 | import 全部成功 | ✅（2026-09-05） |
| **M0.6 认证占位** | `app/core/security.py` 三角色定义 + `get_current_user` 占位（开发期固定 admin）+ `require_role` 依赖工厂 | 占位可运行，OA SSO 替换点已标注 TODO | ✅（2026-09-05） |
| **M0.7 审计占位** | `app/core/audit.py` `audit_log()` 函数（四元组结构，当前输出日志，数据库表建好后换落库） | 函数可调用，返回完整四元组 | ✅（2026-09-05） |
| **M0.8 前端 demo 复用** | `app/static/demo/` 从 Odoo 版原样复制（index.html 66.6KB + check.py + README.md），FastAPI 挂载 `/static` 路由，根路径 `/` 自动跳转 demo | demo 可访问（HTTP 200，68198 字节，标题含"深空科技"）；10 个路由注册正常 | ✅（2026-09-05） |
| **M0.9 文档适配** | 4 份文档从 Odoo 版适配为 FastAPI 版：架构设计.md（v7.0）、产品设计文档.md（v6.3）、README.md、AGENTS.md。业务契约 100% 继承，技术栈/测试命令/里程碑适配 | 4 份文件完整；原 Odoo 版文档未改动 | ✅（2026-09-05） |
| **M0.10 领域资产抽取** | ①`data/field_spec.py`：A/B/C 字段分层常量（A 21/B 6/C 10）+ B_DEFAULTS + DATA_SOURCE_TYPES + PRICE_STAT_TYPES + 分层辅助函数，从 Odoo boq_item.py:31 与 upsert_service.py:27-46 抽取，零框架依赖；②`docs/reference/`：M1–M4 + M1.5 设计稿 5 份复制（185.9KB），业务契约可继承、Odoo 实施章节须重写；③`docs/reference/security_test_spec.md`：S1–S9 安全用例契约提取，作为 FastAPI 版 tests/ 的验收标准 | field_spec.py import 成功、A/B/C 交集断言通过、分层函数正确；pure_tests 90 passed | ✅（2026-09-05） |

### 4.2 M1 数据模型与基础（🔄 待启动）

| 任务 | 负责 | 范围 | 验收判据 | 状态 |
|---|---|---|---|---|
| **M1.1 SQLAlchemy 模型** | M1-数据模型 | 四核心模型（boq_item/import_batch/material_dict/audit_log）+ biz_id + 声明式索引 + A/B/C 字段标注 | 模型可 create_all，字段与 Odoo 版设计稿一致，迁移脚本可重复执行 | ✅ 2026-09-05 |
| **M1.2 编码解析与 match_key** | M1-数据模型 | 调用 `data/gb_code.py` 纯函数，compute 改为 SQLAlchemy hybrid_property 或 Service 层计算 | pure_tests 90 条仍全绿；三判据实证（同码 2013/2024 分键、截位、Z 前缀） | ⏸ |
| **M1.3 分层 Upsert 与孤儿** | M2-导入服务（M1 后做） | A 类可覆盖、B 类禁静默覆盖、孤儿检测与回灌 | 同 Odoo 版 S3/S4 用例语义 | ⏸（依赖 M1.1） |
| **M1.4 审计落库** | M1-数据模型 | `audit_log` 表 append-only，`core/audit.py` 从日志输出换为落库；写操作中间件自动记录 | S5/S9 用例：所有写操作有审计记录、四元组完整 | ⏸（依赖 M1.1） |
| **M1.5 权限体系** | M1-权限认证 | 三角色（admin/estimator/viewer）+ 字段级只读（B 类字段无 reason 拒绝）+ 依赖注入实现 | 权限用例：viewer 不可导出、estimator 不可改 B 类、admin 全权限 | ⏸ |
| **M1.6 测试与验收** | M1.6-测试验收 | pure_tests 90 条 + FastAPI 集成测试（模型/审计/权限） | 全部零失败；模型与 Odoo 版字段逐项核对 | ⏸ |

### 4.3 后续里程碑

| 里程碑 | 范围 | 设计稿来源 | 前置 | 状态 |
|---|---|---|---|---|
| **M2 导入与查询** | 导入向导（预览不写库、确认才入库）、查询、导出（15 列+>1000 行闸门）、批次软删/还原/硬删（30 天宽限+CSV 快照+B 类闸门+审计） | 原 Odoo 版 M2模块设计.md v1.4（业务契约继承，实现重写） | M1 全部完成 | ⏸ |
| **M2.5 UI 页面** | 数据概览、清单检索、导入向导+批次管理、单价分析+物料字典+匹配确认+系统设置——深空科技风格 | 原 demo/style-lab/index.html（视觉规格继承），模板实现 | M2 | ⏸ |
| **M3 单价分析** | 同类项聚合分析、匹配向导（rapidfuzz 候选+人工确认）、质量仪表盘、cost_catalog 占位；**匹配效率优化（2026-09-07 完成）**：①降低自动确认阈值（98/10→90/15）②同义词自动扩展（学习机制）③后台预匹配（match_cache+异步计算）④TF-IDF 语义匹配 ⑤FAISS 向量检索 ⑥学习引擎权重自适应（三算法融合） | 原 Odoo 版 M3模块设计.md v1.4 | M2 | ✅ 2026-09-07（匹配确认+效率优化全部完成） |
| **M4 成本库与语义检索** | 成本库 + LanceDB 语义检索（embedding_version/去重键/B 类审计） | 原 Odoo 版 M4模块设计.md v1.4 | M3 | 📋 |
| **OA SSO 对接** | 易达 ECMS 统一登录对接，替换 `get_current_user` | 待厂商提供 SSO 协议文档 | 厂商文档到位 | ⏸（独立线，不阻塞 M1–M4） |
| **数据迁移** | Odoo 版 `zaojia_db` 数据 ETL 至新库（或新表），三重校验（行数/B 类逐字段/SHA256） | 迁移方案待 M1 模型定稿后出 | M1.1 | ⏸ |
| **P3 远期扩展** | 4D 排期、人材机分析 | — | M4 后 | 🧊 |

### 4.4 总控评审记录（滚动追加）

| 日期 | 对象 | 结论 |
|---|---|---|
| 2026-09-05 | M0 迁移准备（骨架+纯函数+测试） | 通过：pure_tests 90 passed / FastAPI app 加载成功 / 4 路由注册 / 依赖全装 / Odoo 版 tag+备份完成。认证与审计为占位，OA 对接到位后替换。 |
| 2026-09-07 | M3 匹配确认 + 匹配效率优化 | 通过：全量测试 455 passed / 三算法融合（rapidfuzz+TF-IDF+FAISS）/ 学习引擎权重自适应 / 后台预匹配缓存 / 同义词自动扩展 / 自动确认阈值降低（98/10→90/15）。预期 10 万条数据下人工确认量从 100% 降至 10-20%。 |

## 5. 评审清单（总控验收子 Agent 产出，逐条核对）

1. **契约符合**：字段/模型/Service 行为与 M1/M2/M3/M4 设计稿对应章节一致；**引用章节须核对原文**，禁止沿用二手引用；
2. **越权检查**：`git diff` 只触碰该子 Agent 授权范围文件（§3.3）；
3. **接口统一**：入参结构化、出参统一信封、错误码前缀正确、无框架内部对象泄漏到返回值；
4. **测试**：§9 登记命令跑通零失败；新改动有对应测试（pure_tests/ 或 FastAPI 集成）；
5. **数据安全**：涉导入/删除/覆盖/导出的改动，必须过安全用例 + 源文件只读校验（无 `wb.save(` 写源文件）+ 敏感文件不入仓校验；
6. **文档回写**：实现与设计稿有偏差时，须回写设计稿偏差表，禁止只改代码不改文档；
7. **commit 纪律**：一改动一 commit；AGENTS.md 两条硬规则（commit + 测试）缺一不验收；
8. **交付前审查**：调用 Self-Improving + Proactive Agent 复核后方可宣告完成。

## 6. 变更提案流程

1. 子 Agent 发现需要改架构/表结构/接口 → 提交【变更提案】：动机、影响面、替代方案、迁移成本；
2. 总控评估 → 与现有契约冲突分析 → 裁决；
3. **确认后由总控回写本文档 §2/§4 + 对应设计稿**，并记录到 §7 ADR；
4. 未回写前实施 = 越权，回滚。

## 7. 设计决策记录（ADR）

| # | 决策 | 内容 | 依据 | 状态 |
|---|---|---|---|---|
| ADR-F001 | 平台从 Odoo 19 迁移至 FastAPI | 业务契约与纯函数 100% 继承；Odoo 框架代码全部重写；动因：Odoo 19 框架复杂度高、AI 写代码 bug 率高、前端定制困难、团队共享与公司网页集成需求 | 2026-09-05 用户决策（多轮评估：Odoo→NocoBase→Python数据栈→FastAPI） | 已确认，M0 落地 |
| ADR-F002 | 纯函数层原样复用零改动 | `data/` 720 行从 Odoo 版逐字节复制，不做任何修改；pure_tests 90 条作为迁移无损的回归基线 | 纯函数与框架解耦，改动无收益且引入风险 | 已确认，M0.3/M0.4 验证 |
| ADR-F003 | OA 统一登录（易达 ECMS）作为认证源 | 门户不建独立账号体系（开发期占位除外）；OA SSO 协议待厂商确认（OIDC/OAuth2/CAS/私有）；`get_current_user` 为唯一替换点 | 公司已有完整 OA 系统，团队共享需求要求统一登录 | 已确认方向，协议待厂商文档 |
| ADR-F004 | 三角色权限体系继承 | admin/estimator/viewer 三角色，与 Odoo 版权限三组语义一致；B 类字段无 reason 拒绝写入 | 数据安全铁律不可变 | 已确认，M1.5 落地 |
| ADR-F005 | 审计四元组 append-only 落库 | operator/reason/trace_id/timestamp，audit_log 表只增不改不删 | 数据安全铁律 | 已确认，M1.4 落地 |
| ADR-F006 | 前端从 Jinja2 模板起步，可渐进升级 | M2/M2.5 用 Jinja2 + 少量 JS（AI 最熟、维护成本低）；如公司网页集成要求前后端分离，再升级 Vue/React，FastAPI 纯 API | 非专业程序员团队，模板方案维护成本最低；FastAPI 天然支持两种形态 | 已确认，M2.5 实施时可拍板升级 |
| ADR-F007 | 数据库迁移采用 ETL + 三重校验 | Odoo 版 zaojia_db 数据不直接复用表结构（Odoo 表名/字段有框架前缀），写 ETL 脚本迁移至新表；校验：行数一致 + B 类逐字段 diff + SHA256 校验和 | B 类数据不可重建，迁移必须零丢失 | 已确认方向，M1 模型定稿后出详细方案 |

## 8. 待决事项（需用户/总控拍板）

1. **OA SSO 协议确认**：易达 ECMS 是否支持标准 SSO（OIDC/OAuth2/CAS）？需联系易达客服/实施获取对接文档。**这是 OA 对接线的唯一阻塞项**，不阻塞 M1–M4 开发。
2. **公司网页集成方式**：同域子路径 / 独立子域名 / iframe 嵌入？需确认公司网站技术栈与维护方。
3. **部署环境**：开发期本机 Windows（已就绪）；生产期服务器（Windows/Linux？内网/云？）待定。
4. **数据迁移时机**：M1 模型定稿后立即做，还是 M2 功能完成后做？建议 M1 后先迁一批做验证，M2 后全量切换。
5. **前端升级时机**：Jinja2 模板用到什么阶段升级 Vue/React？建议 M2.5 视觉验收后，如公司网页集成有强需求再升级。
6. **M3/M4 设计稿是否需要适配 FastAPI**：原 M3/M4 设计稿基于 Odoo 框架编写，业务契约可继承，但实施章节（Odoo ORM/views/wizards）需重写为 FastAPI/SQLAlchemy 版本。是否在对应里程碑启动前重写设计稿？
7. **原 Odoo 版下线时机**：FastAPI 版 M2 完成后双轨并行（Odoo 只读 + FastAPI 读写），M3 完成后观察 1 个月再下线 Odoo。是否按此节奏？
8. **PG 服务启动方式固化**：PG 非 Windows 服务、需每次手动 `pg_ctl start`（继承自 Odoo 版环境坑），建议固化为计划任务或开机自启脚本。

## 9. 硬规则（测试命令与安全基线）

### 9.1 测试命令

- **纯函数测试**（任何改动后必跑）：
  ```
  C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe -m pytest pure_tests/ -v
  ```
  判据：121 passed（原90 + field_spec 21 + aliases 新增9），零失败。

- **FastAPI 集成测试**（M1 起，涉及模型/服务/API 的改动必跑）：
  ```
  C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe -m pytest tests/ -v
  ```
  判据：全部 passed，零失败。测试使用独立测试数据库（不碰生产/开发库）。

- **应用启动验证**（改 main.py/路由/配置后必跑）：
  ```
  cd E:\DEEPSEEK学习\zaojia_fastapi
  C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe -c "from app.main import app; print('routes:', len(app.routes))"
  ```
  判据：无 ImportError，路由数符合预期。

### 9.2 数据安全基线（继承自 Odoo 版，不可变）

- **S1–S9 安全用例优先级高于功能用例**，任一失败 = 整体不通过，不得交付；
- **源文件只读**：导入相关代码不得出现对源 Excel 的 `wb.save(`（允许的例外：导出向内存 BytesIO 生成全新文件；测试自建临时夹具）；所有 `load_workbook` 必须带 `read_only=True, data_only=True`；
- **敏感文件不入仓**：`git status` 不得出现 `.xlsx/.xls/.sql/.dump` 文件；
- **B 类字段保护**：无 reason 的 B 类写入必须被拒绝（代码层 + 测试层双重锁死）；
- **硬删闸门**：硬删前必须导出 CSV 快照成功，快照失败不得放行物理删除（Odoo 版 R1 缺陷的教训，本项目从第一天就锁死）；
- **备份恢复演练**：每季度一次 pg_dump 恢复演练，未演练过的备份视为无效。

### 9.3 与原 Odoo 版的联动

- 原 Odoo 版（`E:\DEEPSEEK学习\造价数据库\`）在 FastAPI 版 M2 完成前保持只读运行，作为数据回滚兜底；
- 原 Odoo 版代码仓库不再新增功能，仅修 P0 安全缺陷；
- 原 Odoo 版数据库 `zaojia_db` 在数据迁移完成前不得修改结构（迁移 ETL 依赖原表结构稳定）。

---

### 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-05 | 初版：从 Odoo 19 迁移至 FastAPI 的新项目总控。继承业务契约（A/B/C 分层/聚合键/审计四元组/接口信封/三角色）与纯函数层（720 行原样复用）；技术基线切换为 FastAPI+SQLAlchemy+PG+Jinja2+OA SSO；WBS 划分为 M0（迁移准备✅）/M1（数据模型）/M2（导入查询）/M3（单价分析）/M4（成本库检索）+ OA SSO 独立线 + 数据迁移线；登记 ADR-F001~F007；吸取 Odoo 版教训（测试先行/小步commit/安全用例优先/视觉基线/敏感文件不入仓） |
| v2.5 | 2026-09-06 | **生产库连接故障修复**。根因：app/db.py 中 _db_url = settings.test_database_url or settings.database_url，.env 里 TEST_DATABASE_URL=sqlite:///:memory: 有值，导致 uvicorn 正常启动时连的是 SQLite 内存库而非 PostgreSQL；SQLite 连接跨线程关闭时抛 ProgrammingError，页面捕获后显示'数据读取失败：OperationalError'。修复：去掉 test_database_url 优先级，直接用 database_url；测试隔离全靠 conftest.py 的 dependency_overrides。同时 init_db() 创建缺失的 cost_catalog 表（M4 模型）。验证：424 passed；dashboard KPI 卡片正常渲染 558/0/556/511；uvicorn 日志无 SQLite 线程错误。commit 7dcca68。 |
| v1.1 | 2026-09-05 | 代码审查后批量修复 + M1.1 完成。修复 4 项 P0：①requirements.txt 全量版本锁定+补 rapidfuzz；②pure_tests/test_field_spec.py 新增 21 条全绿；③data/aliases.py 首个非空列 bug 修复（map_headers 改返回三元组+新增 extract_field_value，测试从 9 条扩至 18 条）；④app/core/security.py 从 fail-open 改为 fail-closed（无 token 401/token 不匹配 401/开发期 dev_token 校验）。地基三件套：app/config.py 安全加固（secret_key 必填 env/debug env 控制/TEST_DATABASE_URL/dev_token，.env 绝对路径定位）；app/db.py 引擎层（engine/SessionLocal/Base/get_db/init_db，pool_pre_ping）；tests/ 骨架（conftest.py SQLite 内存库隔离+override get_db，test_models.py 13 条全绿）。SQLAlchemy 四核心模型：boq_item（44列，含 A/B/C 分层标注/外键 RESTRICT/声明式复合索引/data_source_type 必填无默认）、import_batch（23列）、material_dict（13列，三级树自引用）、audit_log（12列，append-only 事件监听禁止 update/delete）。全量验证：pure_tests 121 条 + tests 13 条 = **134 passed**，FastAPI 应用 10 路由正常加载。M1.1 里程碑标记完成。 |

---

> 维护纪律：本文档每次更新须 commit；状态列变更（任务完成/启动）由总控 Agent 执行；子 Agent 不得直接改写本文档，只能通过变更提案。
