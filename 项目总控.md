# 项目总控 · 造价数据门户（FastAPI 版）（唯一事实源）

> 版本：v1.6（2026-09-14：单价分析聚合身份按清单-材料映射显示 + 测试稳定性 551×3 全绿 + .gitignore 行内注释修复）
> 建立日期：2026-09-05 · 维护人：总控 Agent
> 原 Odoo 版总控：`E:\DEEPSEEK学习\造价数据库\项目总控.md`（v2.15，已归档，不再更新）
> 原 Odoo 版代码：git tag `legacy-odoo-final`（commit a93d82c），数据库备份 `E:\DEEPSEEK学习\zaojia_backup\zaojia_db_20260905.dump`

> **本文档是项目目标、架构、分工、任务分解、接口契约、变更决策的唯一事实源。**
> 子 Agent 的一切产出以本文档 + 各设计稿为准；冲突时以本文档的「契约裁决」为准。
> 未经过总控 Agent 确认并回写本文档，任何架构、表结构、接口改动不允许生效。

---

## 1. 项目目标

面向造价从业人员的**工程造价数据门户**：把多省多批次的工程量清单 Excel 安全导入、标准化、聚合成可分析的数据资产，最终支撑单价分析、成本库与语义检索。作为公司网页的一部分，支持团队共享查询。

**核心底线（继承自 Odoo 版，不可变）**：原始造价 Excel 与人工标注（B 类字段）不可重建、不可静默丢失。

**与 Odoo 版的关系**：本项目是 Odoo 19 版（`zaojia_boq` 模块，M0–M3 已完成）的平台迁移重写。业务契约、纯函数算法、数据模型设计 100% 继承；Odoo 框架代码（ORM/views/XML/OWL/SCSS/security CSV）全部重写为 FastAPI + SQLAlchemy + 模板实现。

## 2. 技术架构基线（不可变决策，改动须走变更提案）

| 项 | 决策 |
|---|---|
| 技术栈 | FastAPI + SQLAlchemy 2.0 + PostgreSQL 15 + Jinja2 模板（前端渐进可升级 Vue/React） |
| Python | 系统 Python 3.12（`C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe`） |
| 核心模型 | `boq_item` / `import_batch` / `material_dict` / `audit_log` / `match_cache`（M3 预匹配缓存）/ `cost_catalog`（M4 成本目录）——字段设计继承 Odoo 版，表名去掉 `zaojia_boq_` 前缀 |
| 数据分层 | A 类可重建（导入覆盖）、B 类不可重建（禁删禁静默覆盖）、C 类系统——**与 Odoo 版完全一致** |
| 聚合键 | `match_key` 优先级 dict > std > Z前缀 > 有效国标码 > raw，显式带版本维度；跨行聚合身份 `aggregate_id`（继承 ADR-001） |
| 存储约定 | `price_period` 存当月 1 日；`item_code` 按文本处理（9 位合规 / 12 位截前 9 / 10 位异常映射） |
| 稳定 ID | `biz_id` Char(36) UUID 不可变，API 一律用 `biz_id` |
| 接口信封 | `{success, data, total, warnings, trace_id}`；错误码前缀 `IMPORT_/BOQ_/PRICE_/MATCH_/EVIDENCE_/BATCH_/QUALITY_`（继承 Odoo 版契约） |
| 审计四元组 | 写操作必带 `operator / reason / trace_id / timestamp` 并落 `audit_log`（append-only） |
| 认证 | 公司 OA 统一登录（易达 ECMS，待厂商确认 SSO 协议）；开发期占位独立账号，OA 对接到位后替换 `get_current_user` |
| 角色权限 | 三角色：`admin`（导入/删除/标注/用户管理）/ `estimator`（查询/导出/单价分析，B 类字段只读）/ `viewer`（只读浏览，禁导出） |
| 纯函数层 | `data/` 目录纯函数（gb_code/aliases/unit_normalize/match_score/price_calc/quality_metrics/cost_catalog_gate），**从 Odoo 版原样复制，零改动**，不依赖任何框架；**2026-09-07 新增匹配算法**：`tfidf_matcher.py`（TF-IDF 语义匹配）、`faiss_matcher.py`（FAISS 向量检索）、`learning_engine.py`（学习引擎 v1 权重自适应）、`learning_engine_v2.py`（v2 七项深化能力）、`learning_engine_v3.py`（v3 五项核心能力：概念漂移/多策略集成/学习轨迹可解释/主动学习优化/元学习自动调参）；**2026-09-08 新增 M2 深化**：`file_parser.py`（多格式解析 xlsx/xlsm/xls/csv + 魔数校验）、`field_mapper.py`（字段映射模板 13 标准字段含暂估价 + 自动推荐）、`validation_engine.py`（校验规则引擎 5 种类型/7 个模板/三级分级）、`archive_enhanced.py`（归档增强 ZIP/GZIP 压缩 + 完整性巡检 + 保留策略） |
| 外部检索 | FAISS 向量检索独立于 PG，不侵入；rapidfuzz 仅候选建议、人工确认才回填；**2026-09-07 三算法融合**：rapidfuzz（精确匹配，权重 0.30）+ TF-IDF（语义匹配，权重 0.35）+ FAISS（向量检索，权重 0.35），权重由学习引擎 v3 根据人工确认结果自适应调整 |
| 界面 | 深空科技设计系统（主色 #4D7CFE / 辅色 #22D3EE / 底 #0B0F1A），视觉规格继承 Odoo 版 demo/style-lab，实现重写；**2026-09-08 方案 A 前后端页面分离**：Portal 页（/portal/*，3 页：dashboard/search/price，可选登录，未登录仅已标准化数据可见）+ Admin 页（/admin/*，8 页：dashboard/import/batches/dict/match/quality/cost-catalog/settings，必须登录+角色校验），旧路由 301 重定向 |
| 部署 | 开发期 uvicorn `127.0.0.1:8777`；生产期 Nginx 反向代理 + HTTPS，作为公司网页子路径或子域名；**生产环境安全加固（2026-09-08）**：/docs 与 /openapi.json 生产期禁用，Cookie httponly=True 防 XSS，datetime 全部 timezone-aware |

**从 Odoo 版吸取的教训（本项目强制遵守）**：
1. **测试先行**：安全铁律和核心业务先写自动化用例，再让 AI 写实现；
2. **小步 commit**：一个功能一 commit 一测试，不攒大改（Odoo 版 R1/H3 缺陷均为攒改漏测）；
3. **安全用例优先级最高**：S1–S9 任一失败 = 整体不通过；
4. **避免框架专有坑**：FastAPI/SQLAlchemy 的版本差异、异步陷阱、迁移回滚必须实测，不凭印象；
5. **视觉基线必做**：UI 改动后必须截图对照，不接受"看起来差不多"；
6. **敏感文件不入仓**：xlsx/xls/sql/dump 禁止进 git。

**契约裁决（pytest 范围）**：`pytest` 分两类——`pure_tests/`（纯函数，系统 Python 直接跑）与 `tests/`（FastAPI 集成测试，含 TestClient + 测试数据库），禁止混跑。

## 3. 组织与分工

### 3.1 总控 Agent（本角色）

- 维护本文档；拆解需求、分配子任务；评审子 Agent 产出（§5 评审清单）；
- 受理/裁决变更提案（§6）；维护版本进度（§4 状态列）；**不写大量业务实现代码**；
- 重要的设计决策全部回写本文档，不留在对话上下文。

### 3.2 总控角色铁律（2026-09-04 用户指令，永久生效，继承自 Odoo 版）

总控**只做记录与把控**——维护本文档、状态回写、阶段划分、门禁定义、验收核对与调度派工；**不得亲自修改实现代码**。具体改动（Python/SQLAlchemy/模板/配置）一律由执行 Agent 按阶段（泳道）实施，每阶段独立交付（改动 + 门禁 + commit）。违反即流程违规、交付无效。

### 3.3 子 Agent 编制（命名空间 = 任务编号）

| 子 Agent | 授权范围（可触碰） | 禁止触碰 |
|---|---|---|
| **M1-数据模型** | `app/models/`、`app/core/audit.py`（落库部分）、`tests/test_models.py`、数据库迁移脚本 | services/、api/、模板、data/ 纯函数 |
| **M1-权限认证** | `app/core/security.py`、`app/config.py`（OA SSO 参数）、`tests/test_security.py` | 业务模型、服务、data/ |
| **M2-导入服务** | `app/services/import_service.py`、`app/services/upsert_service.py`、`app/services/archive_service.py`、`tests/test_import.py` | models 字段定义、data/ 纯函数、其他 service |
| **M2-查询导出** | `app/services/query_service.py`、`app/services/batch_service.py`、`app/api/query.py`、`tests/test_query.py` | 导入逻辑、模型字段 |
| **M2-前端页面** | `app/templates/`、`app/static/`（CSS/JS）、`app/api/pages.py` | 服务层逻辑、模型 |
| **M3-单价分析** | `app/services/price_service.py`、`app/services/material_match_service.py`、`app/services/data_quality_service.py`、`tests/test_m3.py`；纯函数 `data/price_calc.py` 等仅调用不修改 | data/ 纯函数（只读调用）、M1/M2 已验收代码 |
| **M4-成本库检索** | `app/models/cost_catalog.py`、`app/services/vector_search_service.py`、LanceDB 集成脚本 | PG 核心表结构、data/ |
| **OA-SSO对接** | `app/core/security.py`（`get_current_user` 替换）、`app/config.py`（OA 参数）、对接文档 | 业务逻辑、模型、其他服务 |
| **Agent-0（集成守门）** | `app/main.py`（路由注册）、`app/__init__.py`、`requirements.txt`、配置文件 | 业务实现代码；交付总控 commit |
| **Lane-视觉基线** | `docs/visual_baseline/`（截图） | 共享文件；不 commit |

> 越权修改其他模块代码 = 评审直接不通过，回滚其改动。

### 3.4 子 Agent 输出规范（强制，不满足 = 评审不通过）

每个子 Agent 完成（或阶段性汇报）时，输出必须包含以下六项，缺一不可：

1. **改动文件清单**：精确到文件 + 改动性质（新增/修改/删除）；
2. **代码/测试产出**：测试随代码走，新逻辑必有对应测试（pure_tests/ 或 FastAPI 集成）；
3. **自验证据**：§9 登记命令的**原样执行记录**（命令行 + 结果摘要，如 `90 passed`），禁止"应该能过"式表述；
4. **契约对照表**：逐条对照本表授权范围与对应设计稿章节，声明"符合/偏差"；
5. **偏差与提案**：与设计稿有任何偏差 → 明确列出并附【变更提案】（动机/影响面/替代方案/迁移成本），**不得先斩后奏**；
6. **引用核对声明**：所引用的设计稿章节号须核对原文（B 类字段、聚合键、存储约定等）。

## 4. 全量任务分解（WBS）

> 状态：✅ 完成 · 🔄 进行中 · ⏸ 未启动（等依赖）· 📋 设计完成待实施 · 🧊 远期

### 4.1 已完成（M0 迁移准备）

| 任务 | 产出 | 验收 | 状态 |
|---|---|---|---|
| **M0.1 Odoo 版归档** | git tag `legacy-odoo-final`（commit a93d82c）+ pg_dump 备份 `zaojia_db_20260905.dump`（4.2MB，8444 TOC 条目） | tag 存在、备份可 pg_restore 读取 | ✅（2026-09-05） |
| **M0.2 FastAPI 骨架** | 项目目录 `zaojia_fastapi/`，含 app/（main/config/core/models/services/api）、data/、pure_tests/、requirements.txt、README.md | `from app.main import app` 加载成功，4 个路由注册（/health、/api/me、/api/public/hello、/api/admin/audit-test） | ✅（2026-09-05） |
| **M0.3 纯函数迁移** | `data/` 8 文件 720 行从 Odoo 版原样复制（aliases/gb_code/unit_normalize/match_score/price_calc/quality_metrics/cost_catalog_gate） | 文件逐字节一致（复制校验） | ✅（2026-09-05） |
| **M0.4 纯函数测试验证** | `pure_tests/` 7 文件从 Odoo 版复制并修正路径（`zaojia_boq/data` → `data`） | **90 passed in 0.25s**，零失败 | ✅（2026-09-05） |
| **M0.5 依赖安装** | fastapi 0.141.1 / uvicorn 0.52.4 / sqlalchemy 2.0.52 / pydantic 2.13.4 | import 全部成功 | ✅（2026-09-05） |
| **M0.6 认证占位** | `app/core/security.py` 三角色定义 + `get_current_user` 占位（开发期固定 admin）+ `require_role` 依赖工厂 | 占位可运行，OA SSO 替换点已标注 TODO | ✅（2026-09-05） |
| **M0.7 审计占位** | `app/core/audit.py` `audit_log()` 函数（四元组结构，当前输出日志，数据库表建好后换落库） | 函数可调用，返回完整四元组 | ✅（2026-09-05） |
| **M0.8 前端 demo 复用** | `app/static/demo/` 从 Odoo 版原样复制（index.html 66.6KB + check.py + README.md），FastAPI 挂载 `/static` 路由，根路径 `/` 自动跳转 demo | demo 可访问（HTTP 200，68198 字节，标题含"深空科技"）；10 个路由注册正常 | ✅（2026-09-05） |
| **M0.9 文档适配** | 4 份文档从 Odoo 版适配为 FastAPI 版：架构设计.md（v7.0）、产品设计文档.md（v6.3）、README.md、AGENTS.md。业务契约 100% 继承，技术栈/测试命令/里程碑适配 | 4 份文件完整；原 Odoo 版文档未改动 | ✅（2026-09-05） |
| **M0.10 领域资产抽取** | ①`data/field_spec.py`：A/B/C 字段分层常量（A 21/B 6/C 10）+ B_DEFAULTS + DATA_SOURCE_TYPES + PRICE_STAT_TYPES + 分层辅助函数，从 Odoo boq_item.py:31 与 upsert_service.py:27-46 抽取，零框架依赖；②`docs/reference/`：M1–M4 + M1.5 设计稿 5 份复制（185.9KB），业务契约可继承、Odoo 实施章节须重写；③`docs/reference/security_test_spec.md`：S1–S9 安全用例契约提取，作为 FastAPI 版 tests/ 的验收标准 | field_spec.py import 成功、A/B/C 交集断言通过、分层函数正确；pure_tests 90 passed | ✅（2026-09-05） |

### 4.2 M1 数据模型与基础（🔄 待启动）

| 任务 | 负责 | 范围 | 验收判据 | 状态 |
|---|---|---|---|---|
| **M1.1 SQLAlchemy 模型** | M1-数据模型 | 四核心模型（boq_item/import_batch/material_dict/audit_log）+ biz_id + 声明式索引 + A/B/C 字段标注 | 模型可 create_all，字段与 Odoo 版设计稿一致，迁移脚本可重复执行 | ✅ 2026-09-05 |
| **M1.2 编码解析与 match_key** | M1-数据模型 | 调用 `data/gb_code.py` 纯函数，compute 改为 SQLAlchemy hybrid_property 或 Service 层计算 | pure_tests 90 条仍全绿；三判据实证（同码 2013/2024 分键、截位、Z 前缀） | ✅ 2026-09-06 |
| **M1.3 分层 Upsert 与孤儿** | M2-导入服务（M1 后做） | A 类可覆盖、B 类禁静默覆盖、孤儿检测与回灌 | 同 Odoo 版 S3/S4 用例语义 | ✅ 2026-09-06 |
| **M1.4 审计落库** | M1-数据模型 | `audit_log` 表 append-only，`core/audit.py` 从日志输出换为落库；写操作中间件自动记录 | S5/S9 用例：所有写操作有审计记录、四元组完整 | ✅ 2026-09-06 |
| **M1.5 权限体系** | M1-权限认证 | 三角色（admin/estimator/viewer）+ 字段级只读（B 类字段无 reason 拒绝）+ 依赖注入实现 | 权限用例：viewer 不可导出、estimator 不可改 B 类、admin 全权限 | ✅ 2026-09-06 |
| **M1.6 测试与验收** | M1.6-测试验收 | pure_tests 90 条 + FastAPI 集成测试（模型/审计/权限） | 全部零失败；模型与 Odoo 版字段逐项核对 | ✅ 2026-09-06 |

### 4.3 后续里程碑

| 里程碑 | 范围 | 设计稿来源 | 前置 | 状态 |
|---|---|---|---|---|
| **M2 导入与查询** | 导入向导（预览不写库、确认才入库）、查询、导出（15 列+>1000 行闸门）、批次软删/还原/硬删（30 天宽限+CSV 快照+B 类闸门+审计）；**M2 深化（2026-09-08 完成）**：多格式解析器（xlsx/xlsm/xls/csv + 魔数校验）、字段映射模板（13 标准字段含暂估价 + 自动推荐）、校验规则引擎（5 种类型/7 个模板/三级分级）、归档增强（ZIP/GZIP 压缩 + 完整性巡检 + 保留策略） | 原 Odoo 版 M2模块设计.md v1.4（业务契约继承，实现重写） | M1 全部完成 | ✅ 2026-09-07（含 M2 深化） |
| **M2.5 UI 页面** | 数据概览、清单检索、导入向导+批次管理、单价分析+物料字典+匹配确认+系统设置——深空科技风格；**方案 A 前后端分离（2026-09-08 完成）**：Portal 页（/portal/*，3 页）+ Admin 页（/admin/*，8 页），旧路由 301 重定向，三角色权限矩阵，数据可见性控制（未登录仅已标准化数据） | 原 demo/style-lab/index.html（视觉规格继承），模板实现 | M2 | ✅ 2026-09-08（含方案 A 分离） |
| **M3 单价分析** | 同类项聚合分析、匹配向导（rapidfuzz 候选+人工确认）、质量仪表盘、cost_catalog 占位；**匹配效率优化（2026-09-07 完成）**：①降低自动确认阈值（98/10→90/15）②同义词自动扩展（学习机制）③后台预匹配（match_cache+异步计算）④TF-IDF 语义匹配 ⑤FAISS 向量检索 ⑥学习引擎权重自适应（三算法融合）⑦学习引擎 v2/v3 深化（概念漂移/多策略集成/元学习自动调参） | 原 Odoo 版 M3模块设计.md v1.4 | M2 | ✅ 2026-09-07（匹配确认+效率优化+学习引擎 v2/v3 全部完成） |
| **M4 成本库与语义检索** | 成本库 + 成本目录聚合（cost_catalog 表 + cost_migration_service 从 boq_item 聚合） | 原 Odoo 版 M4模块设计.md v1.4 | M3 | ✅ 2026-09-07 |
| **M5 自动指标生成** | 从工程量清单原始数据自动生成 E 经济指标 / Q 工程量指标 / D 单价指标全套成果；V6 编码方案（E/Q/D 三套独立 8 位编码，对齐湘标 2.0）；DB11/T1711-2019 标准成果输出；Q×D≈E 偏差校验；编码明细表 + 三类指标汇总表 + DB11 标准 Sheet 集 + 公司台账 + 校验报告 | 豆包对话「工程量单价指标汇总及经济指标编码更新」（https://www.doubao.com/thread/xAo9xgR9piyKrbyoB）；V6 编码方案；DB11/T1711-2019 标准 | M4 完成 + V6 编码字典入库 | 📋 设计完成待实施（详见 §4.5 M5 详细规划） |
| **OA SSO 对接** | 易达 ECMS 统一登录对接，替换 `get_current_user`；**对接方案已完成（2026-09-08）**：docs/OA_SSO对接方案.md，含认证流程设计、角色映射规则、后端实现结构、安全考虑、实施步骤（7 阶段 8-10 天）、备选方案 | 待厂商提供 SSO 协议文档 | 厂商文档到位 | 📋（方案完成，待厂商文档） |
| **数据迁移** | Odoo 版 `zaojia_db` 数据 ETL 至新库（或新表），三重校验（行数/B 类逐字段/SHA256） | 迁移方案待 M1 模型定稿后出 | M1.1 | ⏸ |
| **P3 远期扩展** | 4D 排期、人材机分析 | — | M4 后 | 🧊 |

### 4.4 M5 自动指标生成 · 详细规划

> **来源**：豆包对话「工程量单价指标汇总及经济指标编码更新」（https://www.doubao.com/thread/xAo9xgR9piyKrbyoB）
> **核心价值**：从已入库的工程量清单数据，一键生成符合 DB11/T1711-2019 标准的经济指标/工程量指标/单价指标全套成果，替代人工汇总，提升指标编制效率。

#### 4.4.1 功能概述

| 项 | 说明 |
|---|---|
| **输入** | 已入库的 boq_item 清单数据（按批次/项目筛选）+ 项目元数据（建筑面积 S、工程类型、结构类型等） |
| **编码方案** | V6 编码方案（对齐湘标 2.0）：E 经济指标编码 / Q 工程量指标编码 / D 单价指标编码，三套独立 8 位编码，同名称分部 E/Q/D 编码不同 |
| **输出物** | ①编码明细表（逐条清单挂 E/Q/D 编码）②三类指标汇总表（E/Q/D 分组统计）③DB11 标准成果 Sheet 集（项目概况/特征描述表/单方造价表/主要工程量表/单价指标表/总投资采集表）④公司台账（项目情况一览表）⑤校验报告（Q×D≈E 偏差校验） |
| **标准依据** | DB11/T 1711-2019《建设工程造价技术经济指标采集标准》3.0.4 精度要求；湘标 2.0 编码体系 |
| **适用对象** | 房建安装单位工程（建筑面积 S 已知） |

#### 4.4.2 核心算法

- **E 经济指标**（按 E 编码分组）：分部合价 = Σ清单合价；经济指标（元/㎡）= 分部合价 ÷ 建筑面积 S；占比 = 分部合价 ÷ 项目总合价。精度：E 保留 2 位小数，占比保留 4 位小数后转百分比。
- **Q 工程量指标**（按 Q 编码分组）：分部总工程量 = Σ清单工程量；工程量指标 = 分部总工程量 ÷ 建筑面积 S。精度：Q 保留 3 位小数，单位：原单位/㎡。
- **D 单价指标**（按 D 编码分组，加权平均）：加权平均单价 = 分部总合价 ÷ 分部总工程量。精度：D 保留 2 位小数，单位：元/原单位。
- **Q×D≈E 偏差校验**：偏差率 = |Q×D − E| ÷ E × 100%；≤1% 合格；1-5% 注意（多计量单位混合）；>5% 异常（回溯编码挂接/分组口径）。

#### 4.4.3 处理流程（六阶段）

1. **数据清洗**：过滤无效行（空行/合计行/备注行）+ 字段标准化 + 数据类型转换 + 合价校验（误差>0.5%标记异常）
2. **项目元数据录入**：建筑面积 S（核心分母，不可为0）+ 工程名称/类型/结构/计价依据/造价类型/开竣工/合同信息
3. **编码匹配**：根据【项目名称+项目特征】匹配 V6 编码字典（复用 M3 三算法融合框架）；E/Q/D 三套独立编码；未匹配标记【待补编码】人工复核
4. **指标计算**：E 经济指标 + Q 工程量指标 + D 单价指标（三类可并行计算）
5. **偏差校验**：Q×D≈E 偏差率计算 + 三级判定 + 异常项回溯根因
6. **成果输出**：编码明细表 + 三类指标汇总表 + DB11 标准 Sheet 集（6类）+ 公司台账 + 校验报告

#### 4.4.4 任务分解（WBS）

| 任务 | 负责 | 依赖 | 预计工作量 | 状态 |
|---|---|---|---|---|
| M5.1 V6 编码字典入库 | M5-指标生成 | 无 | 3-5 天 | ⏸ |
| M5.2 项目元数据模型 | M5-指标生成 | M5.1 | 2-3 天 | ⏸ |
| M5.3 编码匹配服务 | M5-指标生成 | M5.1 | 5-7 天 | ⏸ |
| M5.4 指标计算引擎 | M5-指标生成 | M5.3 | 5-7 天 | ⏸ |
| M5.5 偏差校验与报告 | M5-指标生成 | M5.4 | 3-5 天 | ⏸ |
| M5.6 成果导出（Excel） | M5-指标生成 | M5.5 | 5-7 天 | ⏸ |
| M5.7 指标生成页面 | M2-前端页面 | M5.6 | 5-7 天 | ⏸ |
| M5.8 测试与验收 | M5-指标生成 | M5.7 | 3-5 天 | ⏸ |

**总预计工作量**：31-46 天（6-9 周），并行压缩后 4-6 周。

#### 4.4.5 可复用经验

- **匹配算法复用**（最高价值）：M3 三算法融合（rapidfuzz+TF-IDF+FAISS）+ 学习引擎 v3，编码字典替代物料字典，预计节省 500+ 行代码
- **数据清洗复用**：M2 字段映射模板、表头别名映射、单位归一化，预计节省 300+ 行代码
- **导出框架复用**：M2 Excel 导出（>1000行二次确认、多Sheet支持），预计节省 200+ 行代码
- **工程化复用**：pytest/Alembic/CI/安全用例体系全部直接复用，零额外成本
- **整体复用率**：直接复用现有代码 ~39%，考虑框架/设计模式/算法思路的间接复用可达 60-70%

#### 4.4.6 详细文档索引

完整规划文档已存档至 `docs/m5_indicator/`：

| 文件 | 内容 |
|---|---|
| `00_豆包对话原文存档.md` | 豆包对话完整内容（原始素材） |
| `01_V6编码方案说明.md` | V6 编码方案（E/Q/D 三套编码、编码结构、专业码、93个分部） |
| `02_核心算法与公式.md` | E/Q/D 计算公式、精度要求、偏差校验、纯函数实现规划 |
| `03_处理流程六阶段.md` | 数据清洗→元数据→编码匹配→指标计算→偏差校验→成果输出，含可复用组件 |
| `04_DB11标准要点.md` | DB11/T 1711-2019 标准要点（工程类型划分、精度要求、表格体系） |
| `05_可复用经验总结.md` | 从 M0-M4 可复用的组件、算法、框架经验，复用率估算 |
| `06_前期规划与任务分解.md` | WBS 任务分解、依赖关系、里程碑、风险点、资源需求 |
| `07_输出物模板规格.md` | 编码明细表/汇总表/DB11标准Sheet集/台账/校验报告的字段规格 |

### 4.5 总控评审记录（滚动追加）

| 日期 | 对象 | 结论 |
|---|---|---|
| 2026-09-05 | M0 迁移准备（骨架+纯函数+测试） | 通过：pure_tests 90 passed / FastAPI app 加载成功 / 4 路由注册 / 依赖全装 / Odoo 版 tag+备份完成。认证与审计为占位，OA 对接到位后替换。 |
| 2026-09-07 | M3 匹配确认 + 匹配效率优化 | 通过：全量测试 455 passed / 三算法融合（rapidfuzz+TF-IDF+FAISS）/ 学习引擎权重自适应 / 后台预匹配缓存 / 同义词自动扩展 / 自动确认阈值降低（98/10→90/15）。预期 10 万条数据下人工确认量从 100% 降至 10-20%。 |
| 2026-09-08 | M1–M4 全部完成 + 方案 A 前后端分离 + M2 深化 + 学习引擎 v2/v3 + 代码库全面架构梳理 + 安全加固 | 通过：全量测试 512 passed（pure_tests 236 + tests 276）/ 方案 A 前后端页面分离（/portal 3页 + /admin 8页，三角色权限矩阵，数据可见性控制）/ M2 深化四大模块（多格式解析/字段映射模板/校验规则引擎/归档增强）/ 学习引擎 v2（7项能力）+ v3（5项核心能力）/ 代码库全面架构梳理（SCOPE→ROUTE→EFFECT→BREAK→SHIP 五阶段）/ 安全加固（生产环境 /docs 禁用、Cookie httponly=True、datetime 全部 timezone-aware 消除 3352 条弃用告警）/ 清理 11 个旧模板。commit 382a7cb 已 push。 |
| 2026-09-08 | 体验增强四模块（S7/N1-N3 + 单价趋势图 ECharts + 收藏 + 标签） | 通过：全量测试 545 passed（新增 33 测试：N1-N3 12 + 收藏 9 + 标签 12）/ S7/N1-N3 反向用例补齐（N1 .xlsm 只读解析+魔数校验、N2 60MB 文件大小限制 413、N3 归档失败不阻断+异常记录）/ 单价趋势图 SVG 升级为 ECharts 5.5 交互版（折线图+离散带+悬停详情）/ 常用项收藏功能（UserFavorite 模型+4 API+幂等）/ 标签分类功能（Tag+ItemTag 模型+8 API+全局标签+用户级隔离）。 |
| 2026-09-09 | 代码审查修复（C1/C3/C9/C10/C11/C15/C16/C17）+ Alembic 版本化迁移（C2）+ S8 备份恢复自动化（C4） | 通过：全量测试 551 passed / C1 硬删闸门测试（6 测试，快照失败拒绝物理删除 fail-closed）/ C3 GitHub Actions CI（3 job：纯函数/集成/安全用例）/ C9 3 处 naive datetime 改为 timezone-aware / C10 测试基线更新（276→309）/ C11 wb.save 例外注释 / C16 pytest security marker + pytest.ini / C17 jieba SyntaxWarning 过滤 / C2 Alembic 1.19.2 引入（初始迁移 10 表，upgrade/downgrade 验证通过，init_db() 改为 alembic upgrade head，测试环境保留 create_all）/ C4 S8 备份恢复自动化脚本 scripts/verify_backup_restore.py（临时库 pg_restore + 表结构/数据/B类字段抽样校验 + 自动清理，RESULT: PASS 判据）。 |
| 2026-09-09 | M5 自动指标生成资料存档 + 总控规划 | 通过：从豆包对话「工程量单价指标汇总及经济指标编码更新」提取 V6 编码方案/E-Q-D 三类指标算法/DB11标准/六阶段流程，存档至 docs/m5_indicator/（9 文件 87KB：README + 00-07）；总控 §4.4 添加 M5 详细规划（功能概述/核心算法/处理流程六阶段/WBS八任务/可复用经验/详细文档索引）；M5 里程碑行已添加至 §4.3。预计 4-6 周完成，复用率 39%（直接）/60-70%（间接）。commit 856ecee。 |

## 5. 评审清单（总控验收子 Agent 产出，逐条核对）

1. **契约符合**：字段/模型/Service 行为与 M1/M2/M3/M4 设计稿对应章节一致；**引用章节须核对原文**，禁止沿用二手引用；
2. **越权检查**：`git diff` 只触碰该子 Agent 授权范围文件（§3.3）；
3. **接口统一**：入参结构化、出参统一信封、错误码前缀正确、无框架内部对象泄漏到返回值；
4. **测试**：§9 登记命令跑通零失败；新改动有对应测试（pure_tests/ 或 FastAPI 集成）；
5. **数据安全**：涉导入/删除/覆盖/导出的改动，必须过安全用例 + 源文件只读校验（无 `wb.save(` 写源文件）+ 敏感文件不入仓校验；
6. **文档回写**：实现与设计稿有偏差时，须回写设计稿偏差表，禁止只改代码不改文档；
7. **commit 纪律**：一改动一 commit；AGENTS.md 两条硬规则（commit + 测试）缺一不验收；
8. **交付前审查**：调用 Self-Improving + Proactive Agent 复核后方可宣告完成。

## 6. 变更提案流程

1. 子 Agent 发现需要改架构/表结构/接口 → 提交【变更提案】：动机、影响面、替代方案、迁移成本；
2. 总控评估 → 与现有契约冲突分析 → 裁决；
3. **确认后由总控回写本文档 §2/§4 + 对应设计稿**，并记录到 §7 ADR；
4. 未回写前实施 = 越权，回滚。

## 7. 设计决策记录（ADR）

| # | 决策 | 内容 | 依据 | 状态 |
|---|---|---|---|---|
| ADR-F001 | 平台从 Odoo 19 迁移至 FastAPI | 业务契约与纯函数 100% 继承；Odoo 框架代码全部重写；动因：Odoo 19 框架复杂度高、AI 写代码 bug 率高、前端定制困难、团队共享与公司网页集成需求 | 2026-09-05 用户决策（多轮评估：Odoo→NocoBase→Python数据栈→FastAPI） | 已确认，M0 落地 |
| ADR-F002 | 纯函数层原样复用零改动 | `data/` 720 行从 Odoo 版逐字节复制，不做任何修改；pure_tests 90 条作为迁移无损的回归基线 | 纯函数与框架解耦，改动无收益且引入风险 | 已确认，M0.3/M0.4 验证 |
| ADR-F003 | OA 统一登录（易达 ECMS）作为认证源 | 门户不建独立账号体系（开发期占位除外）；OA SSO 协议待厂商确认（OIDC/OAuth2/CAS/私有）；`get_current_user` 为唯一替换点 | 公司已有完整 OA 系统，团队共享需求要求统一登录 | 已确认方向，协议待厂商文档 |
| ADR-F004 | 三角色权限体系继承 | admin/estimator/viewer 三角色，与 Odoo 版权限三组语义一致；B 类字段无 reason 拒绝写入 | 数据安全铁律不可变 | 已确认，M1.5 落地 |
| ADR-F005 | 审计四元组 append-only 落库 | operator/reason/trace_id/timestamp，audit_log 表只增不改不删 | 数据安全铁律 | 已确认，M1.4 落地 |
| ADR-F006 | 前端从 Jinja2 模板起步，可渐进升级 | M2/M2.5 用 Jinja2 + 少量 JS（AI 最熟、维护成本低）；如公司网页集成要求前后端分离，再升级 Vue/React，FastAPI 纯 API | 非专业程序员团队，模板方案维护成本最低；FastAPI 天然支持两种形态 | 已确认，M2.5 实施时可拍板升级 |
| ADR-F007 | 数据库迁移采用 ETL + 三重校验 | Odoo 版 zaojia_db 数据不直接复用表结构（Odoo 表名/字段有框架前缀），写 ETL 脚本迁移至新表；校验：行数一致 + B 类逐字段 diff + SHA256 校验和 | B 类数据不可重建，迁移必须零丢失 | 已确认方向，M1 模型定稿后出详细方案 |
| ADR-F008 | 方案 A 前后端页面分离（单应用 + 路由前缀） | Portal 页（/portal/*，3 页：dashboard/search/price，可选登录，未登录仅已标准化数据可见）+ Admin 页（/admin/*，8 页，必须登录+角色校验），旧路由 301 重定向；三角色权限矩阵（admin 全部/estimator 查询+匹配+字典+批次/viewer 只读禁导出） | 团队共享需求：未登录用户可查询已标准化数据，登录用户可管理；公司网页集成需要前后端分离的清晰边界 | 已确认，2026-09-08 实施完成（commit 53f0bc4/9ebb99f/773f24d） |
| ADR-F009 | 三算法融合 + 学习引擎 v3 权重自适应 | rapidfuzz（编辑距离，权重 0.30）+ TF-IDF（语义匹配，权重 0.35）+ FAISS（向量检索，权重 0.35），三算法融合后 Top-5 候选；学习引擎 v3 根据人工确认结果自适应调整权重，含概念漂移检测/多策略集成/学习轨迹可解释/主动学习优化/元学习自动调参五项核心能力 | 10 万条数据下人工确认量需从 100% 降至 10-20%；单一算法无法兼顾精确匹配与语义匹配；学习机制让系统越用越准 | 已确认，2026-09-07 实施完成（pure_tests 197 passed） |
| ADR-F010 | M2 导入服务深化四大模块 | 多格式解析器（xlsx/xlsm/xls/csv + 魔数校验）、字段映射模板（13 标准字段含暂估价 + 自动推荐）、校验规则引擎（5 种类型/7 个模板/三级分级）、归档增强（ZIP/GZIP 压缩 + 完整性巡检 + 保留策略） | 实际工程中 Excel 格式多样（xls/xlsm/csv），表头不统一，需要字段映射模板；导入前需要校验报告避免垃圾数据入库；归档文件需要压缩节省空间 | 已确认，2026-09-08 实施完成（pure_tests 39 passed，集成到 import_api.py） |
| ADR-F011 | 生产环境安全加固 + datetime 时区感知 | 生产环境禁用 /docs 与 /openapi.json（避免接口结构泄漏）；Cookie httponly=True 防 XSS 窃取令牌（security.py 新增 Cookie 认证回退，支持 Authorization 头和 Cookie 两种方式）；所有 datetime.utcnow() 替换为 datetime.now(timezone.utc)，模型 default 改为 lambda 确保插入时求值，消除 3352 条 DeprecationWarning | 代码库全面架构梳理发现的 P1 风险：生产环境 API 文档公开、Cookie 可被 XSS 窃取、naive datetime 与 aware datetime 混用导致时区语义不一致 | 已确认，2026-09-08 实施完成（commit 382a7cb，全量测试 512 passed） |

## 8. 待决事项（需用户/总控拍板）

1. **OA SSO 协议确认**：易达 ECMS 是否支持标准 SSO（OIDC/OAuth2/CAS）？需联系易达客服/实施获取对接文档。**这是 OA 对接线的唯一阻塞项**，不阻塞 M1–M4 开发。
2. **公司网页集成方式**：同域子路径 / 独立子域名 / iframe 嵌入？需确认公司网站技术栈与维护方。
3. **部署环境**：开发期本机 Windows（已就绪）；生产期服务器（Windows/Linux？内网/云？）待定。
4. **数据迁移时机**：M1 模型定稿后立即做，还是 M2 功能完成后做？建议 M1 后先迁一批做验证，M2 后全量切换。
5. **前端升级时机**：Jinja2 模板用到什么阶段升级 Vue/React？建议 M2.5 视觉验收后，如公司网页集成有强需求再升级。
6. **M3/M4 设计稿是否需要适配 FastAPI**：原 M3/M4 设计稿基于 Odoo 框架编写，业务契约可继承，但实施章节（Odoo ORM/views/wizards）需重写为 FastAPI/SQLAlchemy 版本。是否在对应里程碑启动前重写设计稿？
7. **原 Odoo 版下线时机**：FastAPI 版 M2 完成后双轨并行（Odoo 只读 + FastAPI 读写），M3 完成后观察 1 个月再下线 Odoo。是否按此节奏？
8. **PG 服务启动方式固化**：✅ 已解决（2026-09-14）。通过 `scripts/start_dev.py` 一键启动脚本自动检查 PG，未启动则自动启动并等待就绪后再启动 uvicorn。

## 9. 硬规则（测试命令与安全基线）

### 9.0 开发环境一键启动

- **一键启动**（自动检查 PG + 启动 uvicorn）：
  ```
  cd E:\DEEPSEEK学习\zaojia_fastapi
  python scripts/start_dev.py
  ```
  功能：①自动检查 PostgreSQL 是否运行，未启动则自动 `pg_ctl start` 并等待就绪（最多 30 秒）；②显示数据库数据量；③启动 uvicorn（端口 8777，--reload）；④打印 Admin/Portal 访问链接。
  停止：Ctrl+C 停止 uvicorn；PG 保持运行（下次启动更快）。
  PG 路径：`E:\PostgreSQL\pgsql\bin\pg_ctl.exe`，数据目录：`E:\PostgreSQL\data`。

### 9.1 测试命令

- **纯函数测试**（任何改动后必跑）：
  ```
  C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe -m pytest pure_tests/ -v
  ```
  判据：236 passed（原90 + field_spec 21 + aliases 新增9 + v2 21 + v3 19 + M2深化 39 + 原有37），零失败。

- **FastAPI 集成测试**（M1 起，涉及模型/服务/API 的改动必跑）：
  ```
  C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe -m pytest tests/ -v
  ```
  判据：309 passed（2026-09-09 实测，含 S1-S9 安全用例 + N1-N3 反向用例 + 收藏/标签体验增强），零失败。测试使用独立测试数据库（不碰生产/开发库）。

- **应用启动验证**（改 main.py/路由/配置后必跑）：
  ```
  cd E:\DEEPSEEK学习\zaojia_fastapi
  C:\Users\ht835\AppData\Local\Programs\Python\Python312\python.exe -c "from app.main import app; print('routes:', len(app.routes))"
  ```
  判据：无 ImportError，路由数符合预期。

### 9.2 数据安全基线（继承自 Odoo 版，不可变）

- **S1–S9 安全用例优先级高于功能用例**，任一失败 = 整体不通过，不得交付；
- **源文件只读**：导入相关代码不得出现对源 Excel 的 `wb.save(`（允许的例外：导出向内存 BytesIO 生成全新文件；测试自建临时夹具）；所有 `load_workbook` 必须带 `read_only=True, data_only=True`；
- **敏感文件不入仓**：`git status` 不得出现 `.xlsx/.xls/.sql/.dump` 文件；
- **B 类字段保护**：无 reason 的 B 类写入必须被拒绝（代码层 + 测试层双重锁死）；
- **硬删闸门**：硬删前必须导出 CSV 快照成功，快照失败不得放行物理删除（Odoo 版 R1 缺陷的教训，本项目从第一天就锁死）；
- **备份恢复演练**：每季度一次 pg_dump 恢复演练，未演练过的备份视为无效。

### 9.3 与原 Odoo 版的联动

- 原 Odoo 版（`E:\DEEPSEEK学习\造价数据库\`）在 FastAPI 版 M2 完成前保持只读运行，作为数据回滚兜底；
- 原 Odoo 版代码仓库不再新增功能，仅修 P0 安全缺陷；
- 原 Odoo 版数据库 `zaojia_db` 在数据迁移完成前不得修改结构（迁移 ETL 依赖原表结构稳定）。
---

## 10. 材料字典与清单匹配（M6，2026-09-14 新增）

### 10.1 目标

将中建材料字典（14.2万条，5级分类，3-2-2-3-4编码）与 GB/T 50500-2024 工程量清单项目进行匹配映射，建立"清单项目→材料名称→规格→单价"的关联链路，为单价分析和价格聚类提供数据基础。

### 10.2 数据资产

| 数据 | 规模 | 位置 | 状态 |
|---|---|---|---|
| 中建材料字典原始 | 142,946 条 | `E:\DEEPSEEK学习\_test_data\中建材料字典_网页版\zhongjian_material_dict.json.gz` | ✅ 已读取 |
| 中建材料字典 v5（重构+重编码） | 156,733 条 | `deliverables/material_dict_cleaned\zhongjian_material_dict_v5.json.gz` | ✅ 已完成 |
| v5 编码映射表 | 156,733 条 | `deliverables/material_dict_cleaned\code_mapping_v5.json` | ✅ 已完成 |
| 2024 清单 Excel | 5 个 Sheet，2,382 项目 | `F:\360MoveData\Users\ht835\Desktop\测试文件\GBT50500-2024工程量清单计价.xlsx` | ✅ 已读取 |
| 清单-材料匹配结果 V2 | 2,382 条 | `deliverables\list_material_mapping\list_material_match_results_v2.json` | ✅ 已完成 |
| 清单-材料映射表 | 699 条（精确+高置信） | `deliverables\list_material_mapping\list_material_mapping_v2.json` | ✅ 已完成 |
| 电缆价格聚类测试 V2 | 1,435 条（5期） | `deliverables\price_clustering_test\match_results_v2.json` | ✅ 已完成 |
| 电缆基准价格数据 | 2,043 条 | `data\材料价聚类测试文件\电缆特性修正系数测算报告.json.gz` | ✅ 已读取 |

### 10.3 编码方案（3-2-2-3-4）

| 层级 | 位数 | 范围 | 示例 |
|---|---|---|---|
| 第一层（根，39大类） | 3位 | 001-999 | `I001` 钢材及有色金属 |
| 第二层（大类） | 2位 | 01-99 | `I00101` 型钢 |
| 第三层（中类） | 2位 | 01-99 | `I0010101` 圆钢 |
| 第四层（材料名称） | 3位 | 001-999 | `I0010101002` 圆钢(Q235) |
| 第五层（规格） | 4位 | 0001-9999 | `I00101010020044` 圆钢(Q235) Φ6 |

总长度 15 位（含前缀 I），编码唯一性 100%，层级 1-3 符合率 100%，层级 4 符合率 97.8%，层级 5 符合率 99.5%。

### 10.4 清单-材料匹配算法

**匹配策略（三级递进）**：
1. **名称精确匹配**：清单项目名称 == 材料名称（5.6%）
2. **名称模糊匹配**：rapidfuzz WRatio ≥ 75（38.4%）
3. **项目特征匹配**：从项目特征提取材料关键词（规格/材质），与材料字典匹配（11.7%）

**项目特征解析**：
- 材料关键词识别：80+ 常见材料词（圆钢/电缆/阀门/水泥等）
- 规格提取：Φ/DN/截面/尺寸等正则表达式
- 材质提取：Q235/304/镀锌/不锈钢等
- 规格下钻：匹配到材料名称后，进一步匹配具体规格

**匹配结果**：总匹配率 55.7%（房建 73.6% / 市政 59.4% / 园林 48.5% / 安装 47.6%）。未匹配主要为施工工序类（土石方/脚手架）、设备类（机床/变压器）、植物类，属正常现象。

### 10.5 电缆价格聚类测试（V2 优化版）

**优化措施**：
1. **规格标准化**：统一大小写、乘号（×/x/X→*）、全角半角、Φ/φ
2. **规格别名映射**：WDZA=WDZ-A、ZRC=ZR-C 等 16 组别名
3. **精确匹配优先**：标准化精确匹配 → 别名匹配 → 特性匹配
4. **价格异常检测**：以第四期为正常基准，偏离 >50% 标记异常

**测试结果**：
- 精确匹配率：从 V1 的 0% 提升到 **57.1%**（标准化精确 820 条）
- 总匹配率：99.3%（1,425/1,435）
- 价格异常检测：第二期 84.0% / 第三期 81.2% / 第五期 82.2% / 第六期 86.1% 为非正常价格（第四期为基准）
- 基准电缆数据：2,043 条（天津地区，含阻燃/耐火/低烟无卤/铠装特性标签）

### 10.6 字典页面（已并入主系统，2026-09-14 优化）

**历史**：中建材料字典最初以独立预览服务形式存在（端口 8778，只读 gz 文件）。2026-09-14 完成并入主系统 `/admin/dict` 页面，保留原字典的可添加/编辑/删除功能，数据展示按现有字典树形结构。

**主系统字典页面（/admin/dict）**：
- 数据：material_dict 表（156,733 条，5 级结构）
- 功能：树形浏览、搜索过滤、节点详情、新增/编辑/删除分类（五级 CRUD）
- 性能优化（2026-09-14）：懒加载 + 分页 + 缓存 + 搜索，页面加载从 60s+ 降至 294ms（详见 §10.9）

**独立预览服务（保留，端口 8778）**：
- 脚本：`scripts/preview_material_dict.py`
- 数据：v5 重构数据（156,733 条，只读 gz 文件）
- 功能：树形浏览（左导航 exclude_leaves=1）、搜索、节点详情（11 字段）、规格叶子展开
- 用途：快速预览原始数据，不依赖数据库；主系统字典页面为正式入口
- 启动：`python scripts/preview_material_dict.py`

### 10.7 待办与扩展

| 任务 | 优先级 | 说明 | 状态 |
|---|---|---|---|
| 匹配算法优化（同义词词典） | P1 | 增加行业同义词，提高模糊匹配准确率 | ⏸ 待启动 |
| 未匹配项目人工审核 | P1 | 1,055 条未匹配项目，建立补充映射 | ⏸ 待启动 |
| 扩展到其他材料类型 | P2 | 当前仅电缆测试，扩展到管材/阀门/灯具/水泥等 | 🔄 进行中（已生成多材料类型预览） |
| 材料单价库建设 | P2 | 导入各地信息价，建立材料-价格关联 | ⏸ 待启动 |
| 价格预测模型 | P3 | 基于历史价格数据，建立价格趋势预测 | ⏸ 待启动 |
| 匹配结果入库 | P2 | 将清单-材料映射写入系统数据库 list_material_mapping 表 | ✅ 已完成（2013/2024 双版本，3,446 条） |
| 字典页面并入主系统 | P1 | 中建材料字典并入 /admin/dict，保留 CRUD | ✅ 已完成（2026-09-14） |
| 字典页面性能优化 | P1 | 15 万条数据加载优化，懒加载+分页+缓存 | ✅ 已完成（60s+ → 294ms） |

### 10.8 脚本索引

| 脚本 | 功能 |
|---|---|
| `scripts/preview_material_dict.py` | 材料字典独立预览服务（端口8778） |
| `scripts/clean_material_dict.py` | 数据清洗（名称/规格/单位/层级） |
| `scripts/reconstruct_v3.py` | 5级层级重构 |
| `scripts/fix_issues_v4.py` | 问题修复（红砖重复/单位异常） |
| `scripts/recode_v5.py` | 3-2-2-3-4 重新编码 |
| `scripts/match_list_material_v2.py` | 清单-材料匹配（含项目特征解析） |
| `scripts/test_price_clustering_v2.py` | 电缆价格聚类测试（优化版） |
| `scripts/import_dict_5level.py` | 5 级结构材料字典导入数据库 |
| `scripts/match_list_material_v2_optimized.py` | 清单-材料匹配优化版（5 级递进匹配，匹配率 70%+） |
| `scripts/import_mapping_both_versions.py` | 双版本（2013/2024）匹配结果导入数据库 |
| `scripts/check_dict_perf.py` | 字典表索引与查询性能检查 |

### 10.9 字典页面性能优化（2026-09-14，强制沿用）

**问题**：material_dict 表 156,733 条数据（5 级结构），原页面全量加载 + 递归展开，导致页面加载超时（>60 秒）甚至服务无响应。

**优化方案（六层叠加，强制沿用）**：

| 层级 | 优化措施 | 效果 |
|---|---|---|
| 1. 后端查询 | `get_material_dict_tree` 只加载 l1+l2 两级（约 236 条），不递归展开 l3-l5 | 初始查询从 15 万条 → 236 条 |
| 2. 按需加载 | 新增 `/api/dict/children?parent_id=xxx` API，点击展开时才加载子节点 | 避免一次性加载全部数据 |
| 3. 分页加载 | 子节点 API 支持 `page`/`page_size` 参数（默认 100，最大 500），大节点（1,863 条）分页加载 | 大节点单次渲染从 1,863 → 100 条 |
| 4. 前端缓存 | 已加载的子节点缓存到内存（`cache` 对象），展开/收起不重复请求 | 重复展开零网络开销 |
| 5. 搜索过滤 | 树内搜索框（`#dictTreeSearch`），200ms 防抖，按名称过滤可见节点 | 快速定位目标分类 |
| 6. 数据库索引 | 新增复合索引 `ix_material_dict_parent_name (parent_id, name)`，加速子节点排序查询 | 子节点查询从全表扫描 → 索引扫描 |

**性能数据**：
- 页面初始加载：从超时（>60s）→ **294ms**（提升 200 倍+）
- 页面大小：95KB（仅 l1+l2 节点 HTML）
- 大节点子节点分页加载（100 条）：**101ms**，8.3KB
- 数据库查询本身：1-2ms（索引齐全）

**API 契约（强制沿用）**：
```
GET /api/dict/children?parent_id={id}&page={n}&page_size={n}&keyword={kw}
响应：{
  "children": [{id, name, level, parent_id, hasChild}],
  "total": int,
  "page": int,
  "page_size": int,
  "has_more": bool
}
```

**前端实现要点（强制沿用）**：
- `bindDictTree()` 函数维护 `cache` 对象，key=nodeId，value={page, data, loadedAll, total}
- 点击展开箭头：检查 `data-loaded` 属性，未加载则调用 `loadChildren(nodeId, 1)`
- "加载更多"按钮：`data-load-more` 属性，点击调用 `loadChildren(nodeId, nextPage)`
- 搜索框：`input` 事件 200ms 防抖，遍历 `.tree-node` 按 `textContent` 过滤 `display`

**教训（后续大数据量页面强制遵守）**：
1. **禁止全量加载树形数据**：超过 1000 条的树形结构必须懒加载
2. **禁止递归展开**：后端只返回当前层级，子节点按需请求
3. **大节点必须分页**：单节点子节点超过 100 条必须分页
4. **前端必须缓存**：已加载数据缓存到内存，避免重复请求
5. **复合索引必须建**：(parent_id, name) 复合索引加速子节点排序查询
6. **搜索必须防抖**：输入框搜索至少 200ms 防抖，避免频繁重渲染

---

## 11. 单价分析聚合身份优化 + 测试稳定性（2026-09-14）

### 11.1 单价分析聚合身份友好显示（强制沿用）

**问题**：单价分析"各工程对比"聚合组显示原始 `aggregate_id`（如 `code:unknown:030408001|m`、`dict:646`），不可读；201 条 `match_key_source='dict'` 但 `material_dict_id=None`（Odoo 版旧 ID 在 FastAPI 新字典表不存在）。

**修复（commit a5ad38d）**：
1. **数据修复**：8 条名称精确匹配新材料字典（镀锌钢管/钢管/电力电缆/配电箱等）；193 条未匹配（施工工序类）重新计算为 code 模式。修复后分布：dict=8、std=150、code=1231。
2. **聚合身份四层递进显示**（`page_services.py` 的 `_friendly_agg_name`）：
   - **dict 模式**：材料字典名称 + 分类路径，如 `镀锌钢管（方钢/镀锌钢管）`
   - **code 模式（有映射）**：查 `list_material_mapping` 表显示材料名称 + 国标码 + 清单项目名称，如 `混凝土（010501001 垫层）`
   - **code 模式（无映射）**：9 位国标码 + 清单项目名称，如 `040101001 挖沟槽土方`
   - **std/raw 模式**：标准化名称+规格 / 原始项目名称
3. **映射表利用**：`list_material_mapping`（3,446 条：2013=1,759 + 2024=1,687）批量查询，取相似度最高映射；boq_item 197 个唯一国标码中 166 个（84.3%）可匹配。

**教训**：聚合身份显示必须利用清单-材料映射表（list_item_code → material_name），未匹配的施工工序类（土方/平整场地/栽植等）按国标码+项目名称展示，不新建材料字典项（工序非材料）。

### 11.2 测试稳定性检测（2026-09-14）

**结论：551 passed × 3 次，零失败零波动（无 flaky）**。

| 套件 | 单次 | 3 次复跑 | 判定 |
|---|---|---|---|
| `pure_tests/` | 236 passed（1.85s） | 全绿 | ✅ 稳定 |
| `tests/` | 315 passed（3.64s） | 全绿 | ✅ 稳定 |
| 合计 | 551 passed | 551 × 3 | ✅ 无 flaky |

**测试隔离质量（已验证）**：conftest.py 使用 SQLite 内存库 + StaticPool（TestClient 后台线程共享连接）+ 每测试 rollback 清空表 + override get_db + app.state.db_session_factory 供 AuditMiddleware——不触碰生产 PG 库。

**数据安全铁律校验（已验证）**：
- 源文件只读：`wb.save()` 仅 `query.py:187`（导出向导向内存 BytesIO，允许例外）；`load_workbook` 全部 `read_only=True, data_only=True` ✅
- 敏感文件入仓：`git status --porcelain` 无 xlsx/xls/sql/dump 输出 ✅

**本次修复 .gitignore 行内注释 bug（commit d18d627）**：gitignore **不支持行内注释**，原 `archive/  # 导入文件归档` 被当作完整模式导致规则全部失效（archive/、.mimosa/、*.meta.json 长期未忽略）。修复：注释移至独立行。同时入仓领域资产：清单规范提取数据（GB50500-2013/2024 双版本 json.gz）+ 6 个工具脚本（check_2024_sheets/check_list_files/check_sheets_detail/force_create_table/import_material_dict_to_db/match_list_material_both_versions）。

**教训**：.gitignore 禁止行内注释；提交后必须用 `git status --porcelain` 验证忽略规则实际生效（不生效即规则书写错误）。

---

### 版本历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v1.0 | 2026-09-05 | 初版：从 Odoo 19 迁移至 FastAPI 的新项目总控。继承业务契约（A/B/C 分层/聚合键/审计四元组/接口信封/三角色）与纯函数层（720 行原样复用）；技术基线切换为 FastAPI+SQLAlchemy+PG+Jinja2+OA SSO；WBS 划分为 M0（迁移准备✅）/M1（数据模型）/M2（导入查询）/M3（单价分析）/M4（成本库检索）+ OA SSO 独立线 + 数据迁移线；登记 ADR-F001~F007；吸取 Odoo 版教训（测试先行/小步commit/安全用例优先/视觉基线/敏感文件不入仓） |
| v2.5 | 2026-09-06 | **生产库连接故障修复**。根因：app/db.py 中 _db_url = settings.test_database_url or settings.database_url，.env 里 TEST_DATABASE_URL=sqlite:///:memory: 有值，导致 uvicorn 正常启动时连的是 SQLite 内存库而非 PostgreSQL；SQLite 连接跨线程关闭时抛 ProgrammingError，页面捕获后显示'数据读取失败：OperationalError'。修复：去掉 test_database_url 优先级，直接用 database_url；测试隔离全靠 conftest.py 的 dependency_overrides。同时 init_db() 创建缺失的 cost_catalog 表（M4 模型）。验证：424 passed；dashboard KPI 卡片正常渲染 558/0/556/511；uvicorn 日志无 SQLite 线程错误。commit 7dcca68。 |
| v1.1 | 2026-09-05 | 代码审查后批量修复 + M1.1 完成。修复 4 项 P0：①requirements.txt 全量版本锁定+补 rapidfuzz；②pure_tests/test_field_spec.py 新增 21 条全绿；③data/aliases.py 首个非空列 bug 修复（map_headers 改返回三元组+新增 extract_field_value，测试从 9 条扩至 18 条）；④app/core/security.py 从 fail-open 改为 fail-closed（无 token 401/token 不匹配 401/开发期 dev_token 校验）。地基三件套：app/config.py 安全加固（secret_key 必填 env/debug env 控制/TEST_DATABASE_URL/dev_token，.env 绝对路径定位）；app/db.py 引擎层（engine/SessionLocal/Base/get_db/init_db，pool_pre_ping）；tests/ 骨架（conftest.py SQLite 内存库隔离+override get_db，test_models.py 13 条全绿）。SQLAlchemy 四核心模型：boq_item（44列，含 A/B/C 分层标注/外键 RESTRICT/声明式复合索引/data_source_type 必填无默认）、import_batch（23列）、material_dict（13列，三级树自引用）、audit_log（12列，append-only 事件监听禁止 update/delete）。全量验证：pure_tests 121 条 + tests 13 条 = **134 passed**，FastAPI 应用 10 路由正常加载。M1.1 里程碑标记完成。 |
| v1.2 | 2026-09-06 | **M1 全部完成 + 生产库连接故障修复**。M1.2 编码解析与 match_key（gb_code 纯函数集成，三判据实证）、M1.3 分层 Upsert 与孤儿（A类覆盖/B类禁静默覆盖/孤儿检测回灌，S3/S4 用例语义）、M1.4 审计落库（audit_log append-only + AuditMiddleware 写操作自动记录 + operator 真实用户修复）、M1.5 权限体系（三角色 + require_role 依赖工厂）、M1.6 测试验收（全量零失败）。生产库连接故障根因：app/db.py 中 test_database_url 优先级高于 database_url，.env 里 TEST_DATABASE_URL=sqlite:///:memory: 导致 uvicorn 连 SQLite 内存库而非 PG；修复去掉 test_database_url 优先级，测试隔离全靠 conftest.py dependency_overrides。全量验证：424 passed。commit 7dcca68。 |
| v1.6 | 2026-09-14 | **单价分析聚合身份优化 + 测试稳定性检测 + .gitignore 修复**。①单价分析'各工程对比'聚合身份从原始 aggregate_id 改为友好显示：利用 list_material_mapping 映射表（3,446 条，84.3% 国标码可匹配）显示材料名称+国标码+清单项目名称（如 '混凝土（010501001 垫层）'），修复 Odoo 旧 dict ID 不匹配（201 条中 8 条名称匹配修复、193 条重新计算为 code 模式），四层递进显示（dict/code有映射/code无映射/std/raw）。②测试稳定性检测：pure_tests 236 + tests 315 = **551 passed × 3 次复跑，零失败零波动（无 flaky）**；测试隔离（SQLite 内存库+StaticPool+清空表）与数据安全铁律（源文件只读/敏感文件不入仓）验证通过。③修复 .gitignore 行内注释 bug（gitignore 不支持行内注释导致 archive/、.mimosa/、*.meta.json 规则全部失效），注释移至独立行并验证生效；入仓清单规范提取数据（2013/2024 双版本）与 6 个领域工具脚本。新增 §11 章节。commit a5ad38d + d18d627。 |
| v1.5 | 2026-09-14 | **M6 字典页面性能优化 + 五级结构 + 懒加载 + 分页 + 缓存 + 搜索**。中建材料字典（156,733 条，5 级结构）完成并入主系统 /admin/dict 页面，保留原字典的可添加/编辑/删除功能（五级 CRUD）。针对 15 万条数据全量加载超时（>60s）问题，实施六层优化：①后端只加载 l1+l2 两级（236 条）；②新增 /api/dict/children 按需加载 API；③子节点分页（默认 100 条，大节点 1,863 条分页）；④前端内存缓存已加载节点；⑤树内搜索过滤（200ms 防抖）；⑥新增复合索引 (parent_id, name)。优化效果：页面加载从 60s+ 降至 294ms（提升 200 倍+），大节点子节点分页加载 101ms。新增 §10.9 性能优化章节，记录强制沿用的优化方案和教训。commit 4c74cf3。 |
| v1.4 | 2026-09-14 | **M6 材料字典与清单匹配 + 电缆价格聚类测试 V2 + 中建材料字典 5 级重构与重编码**。中建材料字典（142,946条）完成5级层级重构（根→大类→中类→材料名称→规格，156,733条）和3-2-2-3-4编码方案（总长度15位，编码唯一性100%）。完成2024清单（2,382项目）与材料字典匹配映射，总匹配率55.7%（房建73.6%/市政59.4%/园林48.5%/安装47.6%），匹配策略含名称精确+模糊+项目特征解析（规格/材质提取）。电缆价格聚类测试V2优化：规格标准化（大小写/乘号/全角半角）+16组别名映射（WDZA=WDZ-A等），精确匹配率从0%提升到57.1%，总匹配率99.3%；价格异常检测以第四期为基准，确认其他期均为非正常价格（异常率81-86%）。独立预览服务（端口8778）支持树形浏览+搜索+11字段详情。输出：v5重构数据、编码映射表、清单-材料匹配结果、电缆价格聚类测试报告、HTML预览。新增§10 M6章节。 |
| v1.3 | 2026-09-08 | **M1–M4 全部完成 + 方案 A 前后端分离 + M2 深化 + 学习引擎 v2/v3 + 代码库全面架构梳理 + 安全加固 + datetime 时区改造**。M2 导入与查询全部完成（导入向导/查询/导出/批次软删还原硬删四道锁）；M2.5 UI 页面完成（11 个页面）；M3 单价分析完成（匹配确认+质量仪表盘+三算法融合+学习引擎 v1/v2/v3+后台预匹配缓存）；M4 成本目录完成（cost_catalog 表+cost_migration_service）。方案 A 前后端页面分离：Portal 页（/portal/*，3 页，可选登录，未登录仅已标准化数据可见）+ Admin 页（/admin/*，8 页，必须登录+角色校验），旧路由 301 重定向，三角色权限矩阵。M2 深化四大模块：多格式解析器（xlsx/xlsm/xls/csv+魔数校验）、字段映射模板（13 标准字段含暂估价+自动推荐）、校验规则引擎（5 种类型/7 个模板/三级分级）、归档增强（ZIP/GZIP 压缩+完整性巡检+保留策略）。学习引擎 v2（7 项深化能力）+ v3（5 项核心能力：概念漂移/多策略集成/学习轨迹可解释/主动学习优化/元学习自动调参）。代码库全面架构梳理（SCOPE→ROUTE→EFFECT→BREAK→SHIP 五阶段，93 个 Python 文件，56 个 API 端点，6 张数据表，512 测试全绿）。安全加固：生产环境 /docs 与 /openapi.json 禁用、Cookie httponly=True 防 XSS（security.py 新增 Cookie 认证回退）、datetime 全部 timezone-aware（17 处替换，消除 3352 条 DeprecationWarning）。清理 11 个旧模板（已被 portal/admin 新模板替代）。OA SSO 对接方案完成（docs/OA_SSO对接方案.md，7 阶段 8-10 天）。全量验证：pure_tests 236 + tests 276 = **512 passed**，零失败。commit 382a7cb 已 push。 |

---

> 维护纪律：本文档每次更新须 commit；状态列变更（任务完成/启动）由总控 Agent 执行；子 Agent 不得直接改写本文档，只能通过变更提案。
